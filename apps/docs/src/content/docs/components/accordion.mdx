---
title: Accordion 折叠面板
description: 单展开 / 多展开两种模式，3 种边框样式（bordered / flush / separated），可禁用单项。
---

import DemoFrame from '~/components/DemoFrame.astro';
import BasicVue from '~/components/demos/accordion/BasicVue.vue';
import BasicVueSource from '~/components/demos/accordion/BasicVue.vue?raw';
import VariantsVue from '~/components/demos/accordion/VariantsVue.vue';
import VariantsVueSource from '~/components/demos/accordion/VariantsVue.vue?raw';
import MultipleVue from '~/components/demos/accordion/MultipleVue.vue';
import MultipleVueSource from '~/components/demos/accordion/MultipleVue.vue?raw';
import DisabledVue from '~/components/demos/accordion/DisabledVue.vue';
import DisabledVueSource from '~/components/demos/accordion/DisabledVue.vue?raw';
import CustomSlotsVue from '~/components/demos/accordion/CustomSlotsVue.vue';
import CustomSlotsVueSource from '~/components/demos/accordion/CustomSlotsVue.vue?raw';

## 基础用法

`items` 每项 `{ value, title, content?, disabled? }`。默认 `mode="single"` —— 同时只能开一个，点击当前项会折叠它。

<DemoFrame
  vueCode={`<script setup lang="ts">
import { CfAccordion } from '@chufix-design/vue';

const items = [
  { value: 'q1', title: '问题一', content: '答案一…' },
  { value: 'q2', title: '问题二', content: '答案二…' },
  { value: 'q3', title: '问题三', content: '答案三…' },
];
</script>

<template>
  <CfAccordion :items="items" mode="single" />
</template>`}
  reactCode={`import { CfAccordion } from '@chufix-design/react';

const items = [
  { value: 'q1', title: '问题一', content: '答案一…' },
  { value: 'q2', title: '问题二', content: '答案二…' },
  { value: 'q3', title: '问题三', content: '答案三…' },
];

<CfAccordion items={items} mode="single" />`}

  vueSource={BasicVueSource}>
  <BasicVue client:load />
</DemoFrame>

## 三种边框 variant

- `bordered` —— 整体外框 + 项之间分隔线（默认）
- `flush` —— 没有外框，仅项间细线，融入页面背景
- `separated` —— 每项独立卡片 + 间距，块状层次更明显

<DemoFrame
  vueCode={`<CfAccordion :items="items" variant="bordered" />
<CfAccordion :items="items" variant="flush" />
<CfAccordion :items="items" variant="separated" />`}
  reactCode={`<CfAccordion items={items} variant="bordered" />
<CfAccordion items={items} variant="flush" />
<CfAccordion items={items} variant="separated" />`}

  vueSource={VariantsVueSource}>
  <VariantsVue client:load />
</DemoFrame>

## 多面板展开

`mode="multiple"` 让多个面板可以同时打开 —— 此时 `modelValue` / `defaultOpen` 是 `string[]`。常见于 FAQ 页或筛选侧栏。

<DemoFrame
  vueCode={`<CfAccordion
  :items="items"
  mode="multiple"
  variant="separated"
  :default-open="['q1', 'q3']"
/>`}
  reactCode={`<CfAccordion
  items={items}
  mode="multiple"
  variant="separated"
  defaultValue={['q1', 'q3']}
/>`}

  vueSource={MultipleVueSource}>
  <MultipleVue client:load />
</DemoFrame>

## 禁用单项 + 默认展开

设 `item.disabled = true` 让单个面板不可点击。`defaultOpen` (Vue) / `defaultValue` (React) 控制初始展开。

<DemoFrame
  vueCode={`const items = [
  { value: 'a', title: '可点击的项', content: '正常展开 / 折叠。' },
  { value: 'b', title: '禁用项 — 点不动', content: '此项被锁定。', disabled: true },
  { value: 'c', title: '可点击的项', content: '正常展开 / 折叠。' },
];

<CfAccordion :items="items" :default-open="'a'" />`}
  reactCode={`const items = [
  { value: 'a', title: '可点击的项', content: '正常展开 / 折叠。' },
  { value: 'b', title: '禁用项 — 点不动', content: '此项被锁定。', disabled: true },
  { value: 'c', title: '可点击的项', content: '正常展开 / 折叠。' },
];

<CfAccordion items={items} defaultValue="a" />`}

  vueSource={DisabledVueSource}>
  <DisabledVue client:load />
</DemoFrame>

## 自定义标题与面板内容 (具名插槽)

`item.title` / `item.content` 只能是字符串。需要在标题里加 tag、副标题、图标，或在面板内放表格、列表、表单等富内容时用**动态具名插槽**：

- `#title-${item.value}` —— 自定义该项的标题
- `#${item.value}` —— 自定义该项的面板内容

只有提供了对应插槽的项才走自渲染，其他项继续按 `item.title` / `item.content` 渲染。

<DemoFrame
  vueCode={`<CfAccordion :items="items" variant="separated" :default-open="'shipping'">
  <template #title-shipping>
    运输方式
    <CfTag size="sm" tone="success" variant="soft">免运费</CfTag>
  </template>

  <template #shipping>
    <table>
      <tr><th>地区</th><th>时效</th><th>运费</th></tr>
      <tr><td>本地</td><td>1–2 天</td><td>免</td></tr>
      <tr><td>跨省</td><td>3–5 天</td><td>免</td></tr>
    </table>
  </template>
</CfAccordion>`}
  reactCode={`<CfAccordion
  items={items}
  variant="separated"
  defaultValue="shipping"
  renderTitle={(item) =>
    item.value === 'shipping' ? (
      <>运输方式 <CfTag size="sm" tone="success">免运费</CfTag></>
    ) : item.title
  }
  renderContent={(item) =>
    item.value === 'shipping' ? <ShippingTable /> : item.content
  }
/>`}

  vueSource={CustomSlotsVueSource}>
  <CustomSlotsVue client:load />
</DemoFrame>

## API

### Props

| Prop | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `items` | `Array<{ value, title, content?, disabled? }>` | `[]` | 面板列表 |
| `mode` | `'single' \| 'multiple'` | `'single'` | 同时只能开一个 / 可多开 |
| `variant` | `'bordered' \| 'flush' \| 'separated'` | `'bordered'` | 边框样式 |
| `modelValue` (Vue) / `value` (React) | `string \| string[]` | — | 受控当前展开 |
| `defaultOpen` (Vue) / `defaultValue` (React) | `string \| string[]` | — | 非受控初始展开 |

> `mode="single"` 时受控 / 非受控值是 `string`；`mode="multiple"` 时是 `string[]`。

### Events

| Vue 事件 | React 回调 | 载荷类型 | 说明 |
|---|---|---|---|
| `update:modelValue` | `onChange` | `string \| string[]` | 展开状态变化时触发；`single` 模式是 `string`，`multiple` 模式是 `string[]` |

### Slots (Vue)

| Slot 名 | 说明 |
|---|---|
| `title-${item.value}` | 动态具名 —— 自定义指定项的标题 |
| `${item.value}` | 动态具名 —— 自定义指定项的面板内容 |
