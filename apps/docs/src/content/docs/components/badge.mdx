---
title: Badge 徽标
description: 徽标 —— 独立 pill 或贴在子元素角落，dot / count 两种形态。
---

import DemoFrame from '~/components/DemoFrame.astro';
import BasicVue from '~/components/demos/badge/BasicVue.vue';
import WrapVue from '~/components/demos/badge/WrapVue.vue';
import MaxVue from '~/components/demos/badge/MaxVue.vue';

## 独立 pill

不传 children 时，Badge 是一个独立的小胶囊，可以放在文本旁边。`dot` 模式渲染成纯圆点，没有内容。

<DemoFrame
  vueCode={`<script setup lang="ts">
import { Badge } from '@chufix/vue';
</script>

<template>
  <Badge content="New" tone="primary" />
  <Badge content="3" />
  <Badge content="Beta" tone="info" />
  <Badge content="ok" tone="success" />
  <Badge content="hot" tone="warning" />
  <Badge dot tone="danger" />
</template>`}
  reactCode={`import { Badge } from '@chufix/react';

export default function Demo() {
  return (
    <>
      <Badge content="New" tone="primary" />
      <Badge content="3" />
      <Badge content="Beta" tone="info" />
      <Badge content="ok" tone="success" />
      <Badge content="hot" tone="warning" />
      <Badge dot tone="danger" />
    </>
  );
}`}
>
  <BasicVue client:load />
</DemoFrame>

## 贴附子元素

把内容塞进默认插槽 / children，Badge 自动绝对定位到子元素的角上。`placement` 控制角落位置。

<DemoFrame
  vueCode={`<Badge :content="5">
  <Button variant="outline">收件箱</Button>
</Badge>

<Badge dot tone="success">
  <Avatar name="Chen Qi" />
</Badge>

<Badge dot tone="warning" placement="bottom-right">
  <Avatar name="Anna Lee" />
</Badge>

<Badge content="Pro" tone="info" placement="top-left">
  <Button variant="outline">账户</Button>
</Badge>`}
  reactCode={`<Badge content={5}>
  <Button variant="outline">收件箱</Button>
</Badge>

<Badge dot tone="success">
  <Avatar name="Chen Qi" />
</Badge>

<Badge dot tone="warning" placement="bottom-right">
  <Avatar name="Anna Lee" />
</Badge>

<Badge content="Pro" tone="info" placement="top-left">
  <Button variant="outline">账户</Button>
</Badge>`}
>
  <WrapVue client:load />
</DemoFrame>

## 数字与溢出

`max` 控制溢出阈值，超出渲染成 `${max}+`。`showZero` 让数字 0 也显示（默认隐藏）。

<DemoFrame
  vueCode={`<Badge :content="3">
  <Button>3 条</Button>
</Badge>

<Badge :content="120">  <!-- 默认 max=99，渲染成 99+ -->
  <Button>默认 max=99</Button>
</Badge>

<Badge :content="120" :max="999">
  <Button>max=999</Button>
</Badge>

<Badge :content="0" show-zero tone="success">
  <Button>show-zero</Button>
</Badge>`}
  reactCode={`<Badge content={3}>
  <Button>3 条</Button>
</Badge>

<Badge content={120}>
  <Button>默认 max=99</Button>
</Badge>

<Badge content={120} max={999}>
  <Button>max=999</Button>
</Badge>

<Badge content={0} showZero tone="success">
  <Button>show-zero</Button>
</Badge>`}
>
  <MaxVue client:load />
</DemoFrame>

## API

| Prop        | 类型                                                                     | 默认值        | 说明                                       |
| ----------- | ------------------------------------------------------------------------ | ------------- | ------------------------------------------ |
| `content`   | `string \| number`                                                       | —             | 显示内容；空时 + 非 dot 不渲染             |
| `dot`       | `boolean`                                                                | `false`       | 圆点模式（忽略 content）                   |
| `tone`      | `'neutral' \| 'primary' \| 'success' \| 'warning' \| 'danger' \| 'info'` | `'danger'`    | 语义色                                     |
| `max`       | `number`                                                                 | `99`          | 数字上限，超出渲染为 `${max}+`              |
| `showZero`  | `boolean`                                                                | `false`       | 数字 `0` 默认隐藏，开启后显示              |
| `placement` | `'top-right' \| 'top-left' \| 'bottom-right' \| 'bottom-left'`           | `'top-right'` | wrap 模式下角落位置                        |
| `offset`    | `[number, number]`                                                       | —             | 在 placement 之上叠加 `[x, y]` 像素偏移    |
