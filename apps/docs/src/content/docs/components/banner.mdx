---
title: Banner 全宽提示
description: 顶部全宽通告条 —— 6 种 tone × 2 种 variant，可关闭、可吸顶。
---

import DemoFrame from '~/components/DemoFrame.astro';
import BasicVue from '~/components/demos/banner/BasicVue.vue';
import BasicVueSource from '~/components/demos/banner/BasicVue.vue?raw';
import TonesVue from '~/components/demos/banner/TonesVue.vue';
import TonesVueSource from '~/components/demos/banner/TonesVue.vue?raw';
import SolidVue from '~/components/demos/banner/SolidVue.vue';
import SolidVueSource from '~/components/demos/banner/SolidVue.vue?raw';
import ActionVue from '~/components/demos/banner/ActionVue.vue';
import ActionVueSource from '~/components/demos/banner/ActionVue.vue?raw';
import StickyVue from '~/components/demos/banner/StickyVue.vue';
import StickyVueSource from '~/components/demos/banner/StickyVue.vue?raw';

## 基础用法

最简形态 — 一段公告文案。`Banner` 默认 `variant="subtle"`：浅色底 + 强调色文字，最克制的全宽提示。

<DemoFrame
  vueCode={`<CfBanner tone="info">
  系统将于今晚 22:00 - 22:30 进行维护，部分功能可能不可用。
</CfBanner>`}
  reactCode={`<CfBanner tone="info">
  系统将于今晚 22:00 - 22:30 进行维护，部分功能可能不可用。
</CfBanner>`}

  vueSource={BasicVueSource}>
  <BasicVue client:load />
</DemoFrame>

## 6 种 tone

`tone` —— `info`（中性公告）/ `success`（已完成）/ `warning`（需注意）/ `danger`（紧急）/ `accent`（营销）/ `neutral`（最克制）。

<DemoFrame
  vueCode={`<CfBanner tone="info">info</CfBanner>
<CfBanner tone="success">success</CfBanner>
<CfBanner tone="warning">warning</CfBanner>
<CfBanner tone="danger">danger</CfBanner>
<CfBanner tone="accent">accent</CfBanner>
<CfBanner tone="neutral">neutral</CfBanner>`}
  reactCode={`<CfBanner tone="info">info</CfBanner>
<CfBanner tone="success">success</CfBanner>
<CfBanner tone="warning">warning</CfBanner>
<CfBanner tone="danger">danger</CfBanner>
<CfBanner tone="accent">accent</CfBanner>
<CfBanner tone="neutral">neutral</CfBanner>`}

  vueSource={TonesVueSource}>
  <TonesVue client:load />
</DemoFrame>

## 实色 variant

`variant="solid"` 用 tone 的实色填底、白字，视觉权重最强，适合营销公告或紧急中断。

<DemoFrame
  vueCode={`<CfBanner variant="solid" tone="accent" dismissible>
  新版本 v2.0 上线 — 全新主题与三套密度
  <template #action>
    <CfButton size="sm" variant="ghost">查看更新</CfButton>
  </template>
</CfBanner>`}
  reactCode={`<CfBanner
  variant="solid"
  tone="accent"
  dismissible
  action={<CfButton size="sm" variant="ghost">查看更新</CfButton>}
>
  新版本 v2.0 上线 — 全新主题与三套密度
</CfBanner>`}

  vueSource={SolidVueSource}>
  <SolidVue client:load />
</DemoFrame>

## 关闭按钮 + 操作槽

`dismissible` 在右侧加 × 关闭按钮 — 点击后整个 banner 移除并触发 `dismiss` / `onDismiss`。`action` 槽 / prop 在文案与 × 之间放一个操作按钮（清理 / 查看 / 续费）。

<DemoFrame
  vueCode={`<CfBanner tone="warning" dismissible>
  存储空间已使用 86%，请尽快清理或升级配额。
  <template #action>
    <CfButton size="sm" variant="tertiary">清理</CfButton>
  </template>
</CfBanner>`}
  reactCode={`<CfBanner
  tone="warning"
  dismissible
  action={<CfButton size="sm" variant="tertiary">清理</CfButton>}
>
  存储空间已使用 86%，请尽快清理或升级配额。
</CfBanner>`}

  vueSource={ActionVueSource}>
  <ActionVue client:load />
</DemoFrame>

## 吸顶滚动

`sticky` 给 banner 加 `position: sticky; top: 0`，长页面滚动时它会粘在视口顶部。需要外层容器形成滚动上下文 —— 通常是 `<body>` 自身，也可以是某个带 `overflow: auto` 的祖先。

<DemoFrame
  vueCode={`<CfBanner tone="warning" sticky dismissible>
  存储空间已使用 86%
  <template #action>
    <CfButton size="sm" variant="tertiary">清理</CfButton>
  </template>
</CfBanner>`}
  reactCode={`<CfBanner
  tone="warning"
  sticky
  dismissible
  action={<CfButton size="sm" variant="tertiary">清理</CfButton>}
>
  存储空间已使用 86%
</CfBanner>`}

  vueSource={StickyVueSource}>
  <StickyVue client:load />
</DemoFrame>

## API

### Props

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `tone` | `'info' \| 'success' \| 'warning' \| 'danger' \| 'accent' \| 'neutral'` | `'info'` | 语义色 |
| `variant` | `'subtle' \| 'solid'` | `'subtle'` | 视觉模式 |
| `dismissible` | `boolean` | `false` | 显示关闭 × |
| `icon` | `boolean` | `true` | 是否显示左侧图标（Vue 端可用 `#icon` slot 替换；React 端用 `icon` prop 传节点） |
| `sticky` | `boolean` | `false` | 是否吸顶（`position: sticky`） |

### Events

| Vue 事件 | React 回调 | 载荷类型 | 说明 |
|---|---|---|---|
| `dismiss` | `onDismiss` | — | 用户点击 × 关闭按钮时触发，组件随后自卸 |

### Slots / Children

| Slot | 说明 |
|---|---|
| `default` | 公告正文（React 端：`children`） |
| `action` | 文案与 × 之间的操作按钮 / 链接（React 端：`action` prop） |
| `icon` | 替换默认的左侧图标（React 端：`icon` prop 传 ReactNode） |
