---
title: BarChart 柱状图
description: 单 series 垂直柱状图。
---

import DemoFrame from '~/components/DemoFrame.astro';
import BasicVue from '~/components/demos/barchart/BasicVue.vue';
import BasicVueSource from '~/components/demos/barchart/BasicVue.vue?raw';

## 基础用法

数据通过 props 传入，纯 SVG 渲染，无第三方图表库依赖。
配色取自 `--viz-1..8` token，色盲友好。

<DemoFrame
  vueCode={`<CfBarChart ... />`}
  reactCode={`<CfBarChart ... />`}

  vueSource={BasicVueSource}>
  <BasicVue client:load />
</DemoFrame>


## 颜色

`colorIndex` 0..7 直接选 `--viz-1..8`。

import SeriesColorVue from '~/components/demos/barchart/SeriesColorVue.vue';
import SeriesColorVueSource from '~/components/demos/barchart/SeriesColorVue.vue?raw';

<DemoFrame
  vueCode={`<CfBarChart :data="data" :color-index="0" />
<CfBarChart :data="data" :color-index="3" />`}
  reactCode={`<CfBarChart data={data} colorIndex={0} />
<CfBarChart data={data} colorIndex={3} />`}

  vueSource={SeriesColorVueSource}>
  <SeriesColorVue client:load />
</DemoFrame>

## 横向布局

需要较长分类名、排行或对比列表时，使用 `orientation="horizontal"`。

import HorizontalVue from '~/components/demos/barchart/HorizontalVue.vue';
import HorizontalVueSource from '~/components/demos/barchart/HorizontalVue.vue?raw';

<DemoFrame
  vueCode={`<CfBarChart :data="data" :labels="labels" orientation="horizontal" />`}
  reactCode={`<CfBarChart data={data} labels={labels} orientation="horizontal" />`}

  vueSource={HorizontalVueSource}>
  <HorizontalVue client:load />
</DemoFrame>

## API

### Props

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `data` | `number[]` | `—` | 柱高度数组 |
| `labels` | `string[]` | 自动 `0..n-1` | x 轴标签 |
| `width` | `number` | `480` | SVG 宽度 |
| `height` | `number` | `240` | SVG 高度 |
| `colorIndex` | `0..7` | `0` | 柱体颜色（`--viz-1..8`） |
| `orientation` | `'vertical' \| 'horizontal'` | `'vertical'` | 柱方向 |
| `showGrid` | `boolean` | `true` | 显示网格 |
| `showLabels` | `boolean` | `true` | 显示坐标轴标签 |
| `showTooltip` | `boolean` | `true` | 启用 hover tooltip |
| `valueFormatter` | `(value, payload: BarChartInteractionPayload) => string` | — | tooltip 内值的格式化 |
| `tooltipFormatter` | `(payload: BarChartInteractionPayload) => string` | — | 完整 tooltip HTML 自渲染 |
| `ariaLabel` | `string` | — | 透传给根 `<svg>` 的 `aria-label` |

### Events

| Vue 事件 | React 回调 | 载荷类型 | 说明 |
|---|---|---|---|
| `item-enter` | `onItemEnter` | `BarChartInteractionPayload` | 鼠标进入某柱时触发 |
| `item-leave` | `onItemLeave` | `BarChartInteractionPayload` | 鼠标离开时触发 |

### 类型

```ts
interface BarChartInteractionPayload {
  label: string;
  value: number;
  dataIndex: number;
  colorIndex: number;
  nativeEvent?: MouseEvent;
}
```
