---
title: BulletChart 子弹图
description: 单值进度条 + 目标线 + 质量带，比 Gauge 更适合"对比目标"语义。
---

import DemoFrame from '~/components/DemoFrame.astro';
import BasicVue from '~/components/demos/bulletchart/BasicVue.vue';
import BasicVueSource from '~/components/demos/bulletchart/BasicVue.vue?raw';

## 基础用法

数据通过 props 传入，纯 SVG 渲染，无第三方图表库依赖。
配色取自 `--viz-1..8` token，色盲友好。

<DemoFrame
  vueCode={`<CfBulletChart ... />`}
  reactCode={`<CfBulletChart ... />`}

  vueSource={BasicVueSource}>
  <BasicVue client:load />
</DemoFrame>


## 质量带 + 超过目标

`bands` 三段染色 + `target` 垂直线，超过目标时 value 条会延伸过 target。

import BandsVue from '~/components/demos/bulletchart/BandsVue.vue';
import BandsVueSource from '~/components/demos/bulletchart/BandsVue.vue?raw';

<DemoFrame
  vueCode={`<CfBulletChart :value="92" :target="80" :max="100" :bands="bands" />`}
  reactCode={`<CfBulletChart value={92} target={80} max={100} bands={bands} />`}

  vueSource={BandsVueSource}>
  <BandsVue client:load />
</DemoFrame>

## API

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `value` | `number` | `—` | 当前值 |
| `target` | `number` | `—` | 目标值（垂直线） |
| `max` | `number` | `—` | 最大值 |
| `bands` | `{ upTo: number; tone?: 'error' \| 'warning' \| 'success' }[]` | `[]` | 质量带；从左到右染色，配合 `upTo` 累计 |
| `label` | `string` | `—` | 左侧标签 |
| `width` | `number` | `480` | SVG 宽度 |
| `height` | `number` | `32` | SVG 高度 |
| `ariaLabel` | `string` | `—` | 透传给根 `<svg>` 的 `aria-label` |

### Events

| Vue 事件 | React 回调 | 载荷类型 | 说明 |
|---|---|---|---|
| `click` | `onClick` | `BulletChartInteractionPayload` | 点击 bullet 区域 |
| `item-enter` | `onItemEnter` | `BulletChartInteractionPayload` | 鼠标进入 bullet 区域 |
| `item-leave` | `onItemLeave` | `BulletChartInteractionPayload` | 鼠标离开 bullet 区域 |

### 类型

```ts
interface BulletChartInteractionPayload {
  value: number;
  target: number;
  max: number;
  nativeEvent?: PointerEvent | MouseEvent;
}
```
