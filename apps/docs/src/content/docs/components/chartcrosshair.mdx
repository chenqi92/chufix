---
title: ChartCrosshair 图表十字线
description: 可嵌入任何 SVG 图表的十字定位线 + tooltip 工具。
---

import DemoFrame from '~/components/DemoFrame.astro';
import BasicVue from '~/components/demos/chartcrosshair/BasicVue.vue';
import BasicVueSource from '~/components/demos/chartcrosshair/BasicVue.vue?raw';

## 基础用法

数据通过 props 传入，纯 SVG 渲染，无第三方图表库依赖。
配色取自 `--viz-1..8` token，色盲友好。

<DemoFrame
  vueCode={`<CfChartCrosshair ... />`}
  reactCode={`<CfChartCrosshair ... />`}

  vueSource={BasicVueSource}>
  <BasicVue client:load />
</DemoFrame>

## API

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `visible` | `boolean` | `true` |  |
| `x / y` | `number` | `—` | 像素坐标（在父 SVG 坐标系内） |
| `width / height` | `number` | `—` | 父 SVG 总尺寸（用于线长） |
| `showVertical / showHorizontal` | `boolean` | `true / false` |  |
| `tooltip` | `string` | `—` | 跟随光标的数值标签 |
