---
title: Drawer 抽屉
description: 边缘锚定的滑入面板 —— 4 个方向、tone 变体、可缩放、异步确认、不锁背景模式、命令式服务、多抽屉栈管理。
---

import DemoFrame from '~/components/DemoFrame.astro';
import BasicVue from '~/components/demos/drawer/BasicVue.vue';
import BasicVueSource from '~/components/demos/drawer/BasicVue.vue?raw';
import PlacementVue from '~/components/demos/drawer/PlacementVue.vue';
import PlacementVueSource from '~/components/demos/drawer/PlacementVue.vue?raw';
import SizesVue from '~/components/demos/drawer/SizesVue.vue';
import SizesVueSource from '~/components/demos/drawer/SizesVue.vue?raw';
import NoCloseVue from '~/components/demos/drawer/NoCloseVue.vue';
import NoCloseVueSource from '~/components/demos/drawer/NoCloseVue.vue?raw';
import TonesVue from '~/components/demos/drawer/TonesVue.vue';
import TonesVueSource from '~/components/demos/drawer/TonesVue.vue?raw';
import AsyncVue from '~/components/demos/drawer/AsyncVue.vue';
import AsyncVueSource from '~/components/demos/drawer/AsyncVue.vue?raw';
import ResizableVue from '~/components/demos/drawer/ResizableVue.vue';
import ResizableVueSource from '~/components/demos/drawer/ResizableVue.vue?raw';
import ServiceVue from '~/components/demos/drawer/ServiceVue.vue';
import ServiceVueSource from '~/components/demos/drawer/ServiceVue.vue?raw';
import NoMaskVue from '~/components/demos/drawer/NoMaskVue.vue';
import NoMaskVueSource from '~/components/demos/drawer/NoMaskVue.vue?raw';
import CustomFooterVue from '~/components/demos/drawer/CustomFooterVue.vue';
import CustomFooterVueSource from '~/components/demos/drawer/CustomFooterVue.vue?raw';
import StackedVue from '~/components/demos/drawer/StackedVue.vue';
import StackedVueSource from '~/components/demos/drawer/StackedVue.vue?raw';

## 基础用法

`v-model:open` (Vue) / `open + onOpenChange` (React) 双向绑定。Drawer 共用 Modal 的 portal / focus trap / body 滚动锁 / Esc 关闭基础设施。

<DemoFrame
  vueSource={BasicVueSource}>
  <BasicVue client:only="vue" />
</DemoFrame>

## 4 种滑入方向

`placement` 决定从哪一边滑入 — 右（默认，最常见的详情侧栏）/ 左（移动端导航）/ 顶 / 底（playground 风格的临时操作）。

<DemoFrame
  vueSource={PlacementVueSource}>
  <PlacementVue client:only="vue" />
</DemoFrame>

## 5 档尺寸

`size` 控制抽屉宽度（左右 placement）或高度（上下 placement） — `sm` / `md`（默认）/ `lg` / `xl` / `full`（全屏）。`width` / `height` 也可以传具体像素值覆盖档位。

<DemoFrame
  vueSource={SizesVueSource}>
  <SizesVue client:only="vue" />
</DemoFrame>

## Tone 变体

`tone="info | success | warning | error"` 给 header 加一个圆形 tone 图标 + 强调色。`tone="error"` 时默认 OK 按钮自动切到 danger 红。

<DemoFrame
  vueSource={TonesVueSource}>
  <TonesVue client:only="vue" />
</DemoFrame>

## 内置 OK / Cancel + 异步确认

直接传 `okText` / `cancelText`，组件渲染默认按钮组。`onBeforeOk` 接异步函数：返回 false / 抛错 → 阻止关闭并恢复 loading 态；正常 resolve → 关闭并发 `@ok` 事件。

<DemoFrame
  vueSource={AsyncVueSource}>
  <AsyncVue client:only="vue" />
</DemoFrame>

## 内边缘缩放

`resizable` 让抽屉的内边缘变成拖拽把手 —— 用户可以横向（左右抽屉）或纵向（上下抽屉）调整尺寸。最小 240×160。

<DemoFrame
  vueSource={ResizableVueSource}>
  <ResizableVue client:only="vue" />
</DemoFrame>

## 不锁背景

默认 `mask=true` 渲染遮罩并锁定 body 滚动。设置 `mask={false}` 后抽屉浮在原页面之上，遮罩不渲染、滚动也不锁，适合"参考资料"、"实时日志"这类常驻面板。

<DemoFrame
  vueSource={NoMaskVueSource}>
  <NoMaskVue client:only="vue" />
</DemoFrame>

## 锁死关闭路径

表单 / 长流程里有时不希望误触关闭。三个开关全部置 `false` 让用户必须通过底部按钮显式提交或取消：

- `closeOnOverlay={false}` —— 点击遮罩不关
- `closeOnEsc={false}` —— Esc 不关
- `showClose={false}` —— 隐藏右上角 ×

异步态（`onBeforeOk` 进行中）会自动屏蔽所有关闭路径，不需要手动控制。

<DemoFrame
  vueSource={NoCloseVueSource}>
  <NoCloseVue client:only="vue" />
</DemoFrame>

## 自定义 footer

`footer` 具名插槽接收 `{ ok, cancel, loading }` 三个作用域参数，可以完全自渲染按钮组：插入"保存草稿"等额外动作，同时复用组件内置的 ok/cancel 关闭逻辑与 `onBeforeOk` 等待态。

<DemoFrame
  vueCode={`<CfDrawer v-model:open="open" title="编辑文章" :on-before-ok="publish">
  <p>文章正文…</p>
  <template #footer="{ ok, cancel, loading }">
    <CfButton variant="ghost"    :disabled="loading" @click="cancel">取消</CfButton>
    <CfButton variant="tertiary" :loading="saving"   @click="saveDraft">保存草稿</CfButton>
    <CfButton variant="primary"  :loading="loading"  @click="ok">发布</CfButton>
  </template>
</CfDrawer>`}
  reactCode={`<CfDrawer open={open} onOpenChange={setOpen} title="编辑文章" onBeforeOk={publish}
  footer={({ ok, cancel, loading }) => (
    <>
      <CfButton variant="ghost" disabled={loading} onClick={cancel}>取消</CfButton>
      <CfButton variant="tertiary" loading={saving} onClick={saveDraft}>保存草稿</CfButton>
      <CfButton variant="primary" loading={loading} onClick={ok}>发布</CfButton>
    </>
  )}
>
  <p>文章正文…</p>
</CfDrawer>`}

  vueSource={CustomFooterVueSource}>
  <CustomFooterVue client:only="vue" />
</DemoFrame>

## 多抽屉栈

抽屉之间会自动维护一个 z-index 栈，每打开一层 z-index +10；Esc 只关闭最顶层。可以在一个抽屉内继续打开嵌套抽屉，不必关心层级管理。

<DemoFrame
  vueSource={StackedVueSource}>
  <StackedVue client:only="vue" />
</DemoFrame>

## 命令式服务

`drawer.open / confirm / danger` 直接弹一个抽屉并返回 `Promise<boolean>` —— 不需要任何状态绑定。

```ts
import { drawer } from '@chufix-design/vue'; // React 端：从 @chufix-design/react 导入

const ok = await drawer.confirm({
  title: '设置',
  description: '快速调整服务参数',
  placement: 'right',
  content: SettingsForm, // 字符串 / 组件
  onOk: async () => {
    await api.save(); // throw 或 return false 阻止关闭
  },
});
```

<DemoFrame
  vueSource={ServiceVueSource}>
  <ServiceVue client:only="vue" />
</DemoFrame>

## API

| Prop | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `open` | `boolean` | `false` | 受控开关 |
| `title` | `string` | — | header 标题 |
| `description` | `string` | — | 标题下副标题 |
| `placement` | `'left' \| 'right' \| 'top' \| 'bottom'` | `'right'` | 滑入方向 |
| `size` | `'sm' \| 'md' \| 'lg' \| 'xl' \| 'full'` | `'md'` | 横向→宽度档位；纵向→高度档位 |
| `tone` | `'default' \| 'info' \| 'success' \| 'warning' \| 'error'` | `'default'` | 视觉 tone + 自动图标 |
| `width` | `number \| string` | — | 自定义宽度（左右 placement） |
| `height` | `number \| string` | — | 自定义高度（上下 placement） |
| `resizable` | `boolean` | `false` | 内边缘可拖拽缩放 |
| `mask` | `boolean` | `true` | 是否渲染遮罩并锁滚动 |
| `closeOnOverlay` | `boolean` | `true` | 点遮罩是否关闭 |
| `closeOnEsc` | `boolean` | `true` | Esc 是否关闭 |
| `showClose` | `boolean` | `true` | 右上角 × |
| `footerAlign` | `'start' \| 'center' \| 'end' \| 'space-between'` | `'end'` | footer 对齐 |
| `okText` / `cancelText` | `string` | — | 默认按钮文案；不传则不渲染默认按钮 |
| `okVariant` | `'primary' \| 'danger' \| 'secondary'` | `'primary'` | 确认按钮样式 |
| `onBeforeOk` | `() => boolean \| void \| Promise<...>` | — | 异步钩子；false / throw 阻止关闭 |
| `to` | `string \| Element` | `'body'` | Teleport / Portal 目标 |
| `zIndex` | `number` | 自动栈管理 | 自定义 z-index 起点 |

### 插槽 / 事件

- Vue：`header` / 默认 / `footer` 三个具名插槽。`footer` 插槽接收 `{ ok, cancel, loading }` 用于自渲染按钮。
- React：`header`、`footer`（可传 ReactNode 或 `({ ok, cancel, loading }) => ReactNode`）+ `children`。
- 事件：`update:open` / `close` / `ok` / `cancel`。

### Exposed 方法 (Vue ref)

| 方法 | 说明 |
| --- | --- |
| `close()` | 编程式关闭抽屉（受 `onBeforeOk` 异步态保护） |
| `ok()` | 编程式触发 OK 流程（含 `onBeforeOk` 校验） |

通过 `ref` 拿到组件实例后调用：

```vue
<CfDrawer ref="drawerRef" v-model:open="open" title="设置" />
<CfButton @click="drawerRef?.close()">从外部关闭</CfButton>
```

### 服务方法

| 方法 | tone | 默认按钮 |
| --- | --- | --- |
| `drawer.open(opts)` | 跟 opts 一致 | 跟 opts |
| `drawer.confirm(opts)` | warning | OK + Cancel |
| `drawer.danger(opts)` | error | 红色 OK + Cancel |
