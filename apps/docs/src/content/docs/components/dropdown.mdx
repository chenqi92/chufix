---
title: Dropdown 下拉菜单
description: 下拉菜单 —— Popover + 列表 + 键盘导航。支持分组标题、分隔线、危险操作语义。
---

import DemoFrame from '~/components/DemoFrame.astro';
import BasicVue from '~/components/demos/dropdown/BasicVue.vue';
import BasicVueSource from '~/components/demos/dropdown/BasicVue.vue?raw';
import PlacementVue from '~/components/demos/dropdown/PlacementVue.vue';
import PlacementVueSource from '~/components/demos/dropdown/PlacementVue.vue?raw';
import DisabledVue from '~/components/demos/dropdown/DisabledVue.vue';
import DisabledVueSource from '~/components/demos/dropdown/DisabledVue.vue?raw';
import ControlledVue from '~/components/demos/dropdown/ControlledVue.vue';
import ControlledVueSource from '~/components/demos/dropdown/ControlledVue.vue?raw';
import IconItemsVue from '~/components/demos/dropdown/IconItemsVue.vue';
import IconItemsVueSource from '~/components/demos/dropdown/IconItemsVue.vue?raw';

## 基础用法

`items` 数组式配置，按顺序渲染菜单项；`divider: true` 渲染分隔线，`header: true` 渲染分组标题。键盘 `↑↓` 切换、`Enter` / 空格 选中、`Esc` 关闭、`Home` / `End` 跳到首/末项。

<DemoFrame
  vueCode={`<script setup lang="ts">
import { CfButton, CfDropdown } from '@chufix-design/vue';
import type { DropdownItem } from '@chufix-design/vue';

const items: DropdownItem[] = [
  { key: 'profile', label: '个人资料' },
  { key: 'settings', label: '设置' },
  { divider: true, label: '' },
  { header: true, label: '操作' },
  { key: 'rename', label: '重命名' },
  { key: 'archive', label: '归档', disabled: true },
  { key: 'delete', label: '删除', tone: 'danger' },
];

function onSelect(item: DropdownItem) {
  console.log('selected:', item.key);
}
</script>

<template>
  <CfDropdown :items="items" placement="bottom" @select="onSelect">
    <CfButton variant="secondary">操作菜单 ▾</CfButton>
  </CfDropdown>
</template>`}
  reactCode={`import { CfButton, CfDropdown } from '@chufix-design/react';
import type { DropdownItem } from '@chufix-design/react';

const items: DropdownItem[] = [
  { key: 'profile', label: '个人资料' },
  { key: 'settings', label: '设置' },
  { divider: true, label: '' },
  { header: true, label: '操作' },
  { key: 'rename', label: '重命名' },
  { key: 'archive', label: '归档', disabled: true },
  { key: 'delete', label: '删除', tone: 'danger' },
];

export default function Demo() {
  return (
    <CfDropdown
      items={items}
      placement="bottom"
      onSelect={(item) => console.log('selected:', item.key)}
    >
      <CfButton variant="secondary">操作菜单 ▾</CfButton>
    </CfDropdown>
  );
}`}

  vueSource={BasicVueSource}>
  <BasicVue client:only="vue" />
</DemoFrame>

## Placement 方向

`placement` 决定菜单相对触发元素的位置：`top` / `bottom`（默认）/ `left` / `right`。空间不足时浮层会自动翻转到对侧。

<DemoFrame
  vueCode={`<CfDropdown :items="items" placement="top">    <CfButton>top</CfButton></CfDropdown>
<CfDropdown :items="items" placement="bottom"> <CfButton>bottom</CfButton></CfDropdown>
<CfDropdown :items="items" placement="left">   <CfButton>left</CfButton></CfDropdown>
<CfDropdown :items="items" placement="right">  <CfButton>right</CfButton></CfDropdown>`}
  reactCode={`<CfDropdown items={items} placement="top"><CfButton>top</CfButton></CfDropdown>
<CfDropdown items={items} placement="bottom"><CfButton>bottom</CfButton></CfDropdown>
<CfDropdown items={items} placement="left"><CfButton>left</CfButton></CfDropdown>
<CfDropdown items={items} placement="right"><CfButton>right</CfButton></CfDropdown>`}

  vueSource={PlacementVueSource}>
  <PlacementVue client:only="vue" />
</DemoFrame>

## 禁用状态

`disabled` 让 Dropdown 整体不可触发；通常同时给触发元素加 `disabled` 让视觉反馈一致。

<DemoFrame
  vueCode={`<CfDropdown :items="items">
  <CfButton variant="secondary">正常 ▾</CfButton>
</CfDropdown>
<CfDropdown :items="items" disabled>
  <CfButton variant="secondary" disabled>禁用 ▾</CfButton>
</CfDropdown>`}
  reactCode={`<CfDropdown items={items}>
  <CfButton variant="secondary">正常 ▾</CfButton>
</CfDropdown>
<CfDropdown items={items} disabled>
  <CfButton variant="secondary" disabled>禁用 ▾</CfButton>
</CfDropdown>`}

  vueSource={DisabledVueSource}>
  <DisabledVue client:only="vue" />
</DemoFrame>

## 受控模式

通过 `v-model:open` / `open + onOpenChange` 让父组件接管展开状态。
常用于由外部事件触发（如键盘快捷键、右键菜单、引导提示）。

<DemoFrame
  vueCode={`<script setup lang="ts">
import { ref } from 'vue';
const open = ref(false);
</script>

<template>
  <CfButton @click="open = !open">{{ open ? '关闭' : '打开' }}</CfButton>
  <CfDropdown v-model:open="open" :items="items">
    <CfButton variant="secondary">受控触发 ▾</CfButton>
  </CfDropdown>
</template>`}
  reactCode={`import { useState } from 'react';

export default function Demo() {
  const [open, setOpen] = useState(false);
  return (
    <>
      <CfButton onClick={() => setOpen(!open)}>{open ? '关闭' : '打开'}</CfButton>
      <CfDropdown open={open} onOpenChange={setOpen} items={items}>
        <CfButton variant="secondary">受控触发 ▾</CfButton>
      </CfDropdown>
    </>
  );
}`}

  vueSource={ControlledVueSource}>
  <ControlledVue client:only="vue" />
</DemoFrame>

## 带图标的菜单项

`DropdownItem.icon` 接收一段文本（emoji / 符号），会被原样渲染在 label 左侧。
适合简洁标识；如果需要绘制矢量图标，建议改用嵌套组件方案，把整个菜单项写成自定义 trigger + content 的形式。

<DemoFrame
  vueCode={`const items = [
  { key: 'edit',     label: '编辑',   icon: '✏️' },
  { key: 'copy',     label: '复制',   icon: '📄' },
  { key: 'download', label: '下载',   icon: '⬇️' },
  { divider: true, label: '' },
  { key: 'delete',   label: '删除',   icon: '🗑', tone: 'danger' },
];`}
  reactCode={`const items: DropdownItem[] = [
  { key: 'edit',     label: '编辑',   icon: '✏️' },
  { key: 'copy',     label: '复制',   icon: '📄' },
  { key: 'download', label: '下载',   icon: '⬇️' },
  { divider: true, label: '' },
  { key: 'delete',   label: '删除',   icon: '🗑', tone: 'danger' },
];`}

  vueSource={IconItemsVueSource}>
  <IconItemsVue client:only="vue" />
</DemoFrame>

## API · Props

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `items` | `DropdownItem[]` | `[]` | 菜单项配置数组 |
| `placement` | `'top' \| 'bottom' \| 'left' \| 'right'` | `'bottom'` | 首选位置 |
| `offset` | `number` | `6` | 与触发元素的间距（px） |
| `closeOnSelect` | `boolean` | `true` | 选中后关闭 |
| `disabled` | `boolean` | `false` | 禁用触发 |
| `width` | `number \| string` | `—` | 固定宽度，省略则自适应 |
| `open` | `boolean` | `—` | 受控时使用 |

## API · DropdownItem

| 属性 | 类型 | 说明 |
|---|---|---|
| `key` | `string` | 唯一标识（建议提供，便于 `select` 事件分流） |
| `label` | `string` | 显示文本 |
| `icon` | `string` | 可选前置图标（emoji 或符号） |
| `disabled` | `boolean` | 禁用项 |
| `divider` | `boolean` | 渲染为分隔线 |
| `header` | `boolean` | 渲染为分组标题（不可点） |
| `tone` | `'default' \| 'danger'` | 语义色，`danger` 用于不可逆操作 |

## API · Events

| 事件 | 参数 | 说明 |
|---|---|---|
| `select` (Vue) / `onSelect` (React) | `(item, index)` | 选中某一项时触发 |
| `update:open` (Vue) / `onOpenChange` (React) | `(open: boolean)` | 展开状态变化 |
