---
title: Empty 空状态
description: 列表 / 表格 / 搜索结果为空时的占位组件，可带操作按钮。
---

import DemoFrame from '~/components/DemoFrame.astro';
import BasicVue from '~/components/demos/empty/BasicVue.vue';

## 基础用法

不传 props 显示默认图标和"暂无数据"。`title` / `description` 自定义文案，`action` 插槽放跳转或新建按钮。

<DemoFrame
  vueCode={`<CfEmpty />

<CfEmpty
  title="还没有项目"
  description="创建你的第一个项目来开始使用础件。"
>
  <template #action>
    <CfButton variant="primary" size="sm">+ 新建项目</CfButton>
  </template>
</CfEmpty>`}
  reactCode={`<CfEmpty />

<CfEmpty
  title="还没有项目"
  description="创建你的第一个项目来开始使用础件。"
  action={<CfButton variant="primary" size="sm">+ 新建项目</CfButton>}
/>`}
>
  <BasicVue client:load />
</DemoFrame>

## API

| Prop          | 类型               | 默认值        | 说明              |
| ------------- | ------------------ | ------------- | ----------------- |
| `title`       | `string`           | `'暂无数据'`  | 主文案            |
| `description` | `string`           | —             | 副文案            |
| `size`        | `'sm' \| 'md' \| 'lg'` | `'md'`    | 整体尺寸          |

### 插槽 / Children

- `icon` —— 替换默认 SVG 图标
- `title` / `description` —— 替换文本节点（同名 prop 也可）
- `action` —— 操作按钮区
