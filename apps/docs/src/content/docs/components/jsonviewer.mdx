---
title: JsonViewer JSON 浏览器
description: 折叠式 JSON 树视图，按值类型上色，支持深度展开、类型徽标。
---

import DemoFrame from '~/components/DemoFrame.astro';
import BasicVue from '~/components/demos/jsonviewer/BasicVue.vue';
import BasicVueSource from '~/components/demos/jsonviewer/BasicVue.vue?raw';
import DepthVue from '~/components/demos/jsonviewer/DepthVue.vue';
import DepthVueSource from '~/components/demos/jsonviewer/DepthVue.vue?raw';
import TypesVue from '~/components/demos/jsonviewer/TypesVue.vue';
import TypesVueSource from '~/components/demos/jsonviewer/TypesVue.vue?raw';

## 基础用法

`CfJsonViewer` 接受任意 JS 值作为 `data`。对象 / 数组渲染成可折叠节点，叶子节点按类型上色：字符串绿、数字橙、布尔紫、null/undefined 灰斜体。点击行首三角折叠 / 展开。

<DemoFrame
  vueCode={`<CfJsonViewer :data="data" />`}
  reactCode={`<CfJsonViewer data={data} />`}

  vueSource={BasicVueSource}>
  <BasicVue client:load />
</DemoFrame>

## 深度展开 + 类型徽标

`defaultExpandDepth` 控制初始展开层数（0 = 全折叠，1 = 只展开顶层，2 = 展两层 …）。`showTypes` 在每个叶子值后面挂一个 `string` / `number` / `boolean` 等小徽标 — 调试 schema 时有用。

<DemoFrame
  vueCode={`<CfJsonViewer
  :data="data"
  :default-expand-depth="3"
  show-types
/>`}
  reactCode={`<CfJsonViewer
  data={data}
  defaultExpandDepth={3}
  showTypes
/>`}

  vueSource={DepthVueSource}>
  <DepthVue client:load />
</DemoFrame>

## 全类型样例

下面这份数据覆盖 `string` / `number` / `boolean` / `null` / `undefined` / `array` / `object` 七种类型，可以一眼看清各类型的着色和折叠行为。

<DemoFrame
  vueCode={`<CfJsonViewer :data="data" :default-expand-depth="2" show-types />`}
  reactCode={`<CfJsonViewer data={data} defaultExpandDepth={2} showTypes />`}

  vueSource={TypesVueSource}>
  <TypesVue client:load />
</DemoFrame>

## API

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `data` | `unknown` | `—` | 要渲染的 JSON-like 值，可以是任意 JS 对象 |
| `defaultExpandDepth` | `number` | `1` | 初始展开层数 |
| `showTypes` | `boolean` | `false` | 在叶子节点旁显示类型徽标 |
| `bordered` | `boolean` | `true` | 边框 |
| `lineNumbers` | `boolean` | `false` | 预留 — 行号显示开关 |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | 字号 |

类型导出：

```ts
type JsonValueType =
  | 'null'
  | 'undefined'
  | 'boolean'
  | 'number'
  | 'string'
  | 'array'
  | 'object';
```
