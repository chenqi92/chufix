---
title: LineChart 折线图
description: 多 series 折线图，支持平滑曲线、网格线、x/y 轴标签。
---

import DemoFrame from '~/components/DemoFrame.astro';
import BasicVue from '~/components/demos/linechart/BasicVue.vue';
import BasicVueSource from '~/components/demos/linechart/BasicVue.vue?raw';

## 基础用法

数据通过 props 传入，纯 SVG 渲染，无第三方图表库依赖。
配色取自 `--viz-1..8` token，色盲友好。

<DemoFrame
  vueCode={`<CfLineChart ... />`}
  reactCode={`<CfLineChart ... />`}

  vueSource={BasicVueSource}>
  <BasicVue client:load />
</DemoFrame>


## 平滑曲线

`smooth = true` 走 Catmull-Rom 风格的 cubic Bezier，控制点取相邻两点的差值乘以 0.2 张力，
不会过冲（不会出现拐点处的"半圆"鼓包）。

import SmoothVue from '~/components/demos/linechart/SmoothVue.vue';
import SmoothVueSource from '~/components/demos/linechart/SmoothVue.vue?raw';

<DemoFrame
  vueCode={`<CfLineChart :series="series" smooth />`}
  reactCode={`<CfLineChart series={series} smooth />`}

  vueSource={SmoothVueSource}>
  <SmoothVue client:load />
</DemoFrame>

## 多 series

最多支持 8 条同时显示，配色按 `--viz-1..8` 依次循环。

import MultiSeriesVue from '~/components/demos/linechart/MultiSeriesVue.vue';
import MultiSeriesVueSource from '~/components/demos/linechart/MultiSeriesVue.vue?raw';

<DemoFrame
  vueCode={`<CfLineChart :series="series" smooth />`}
  reactCode={`<CfLineChart series={series} smooth />`}

  vueSource={MultiSeriesVueSource}>
  <MultiSeriesVue client:load />
</DemoFrame>

## API

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `series` | `LineSeries[]` | `—` | &#123; name?, data: number[] &#125;[] |
| `labels` | `string[]` | `自动 0..n-1` | x 轴标签 |
| `width / height` | `number` | `480 / 240` |  |
| `smooth` | `boolean` | `false` | 平滑曲线 |
| `showGrid / showLabels` | `boolean` | `true` |  |
| `yLabelFn` | `(v: number) => string` | `v.toFixed(0)` | y 轴标签格式化 |
| `showLegend` | `boolean` | `true` | 显示图例 |
| `showTooltip` | `boolean` | `true` | 启用 hover tooltip |
| `valueFormatter` | `(value, item: LineChartTooltipItem) => string` | — | tooltip 内单 series 值的格式化 |
| `tooltipFormatter` | `(payload: LineChartInteractionPayload) => string` | — | 完整 tooltip HTML 自渲染（覆盖默认布局） |
| `ariaLabel` | `string` | — | 透传给根 `<svg>` 的 `aria-label` |

### Events

| Vue 事件 | React 回调 | 载荷类型 | 说明 |
|---|---|---|---|
| `item-enter` | `onItemEnter` | `LineChartInteractionPayload` | 鼠标进入某 x 位置时触发；含 `label / dataIndex / items[] / nativeEvent` |
| `item-leave` | `onItemLeave` | `LineChartInteractionPayload` | 鼠标离开图表时触发 |
| `legend-toggle` | `onLegendToggle` | `(index: number, hidden: boolean)` | 用户点击图例切换 series 显隐时触发 |

### 类型

```ts
interface LineChartTooltipItem {
  name?: string;
  value: number;
  colorIndex: number;
  seriesIndex: number;
  dataIndex: number;
}

interface LineChartInteractionPayload {
  label: string;
  dataIndex: number;
  items: LineChartTooltipItem[];
  nativeEvent?: MouseEvent;
}
```
