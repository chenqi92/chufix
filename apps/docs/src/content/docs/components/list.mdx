---
title: List 列表
description: 通用列表，支持选中（单选 / 多选）、分组、卡片化、自定义渲染。
---

import DemoFrame from '~/components/DemoFrame.astro';
import BasicVue from '~/components/demos/list/BasicVue.vue';
import BasicVueSource from '~/components/demos/list/BasicVue.vue?raw';
import GroupedVue from '~/components/demos/list/GroupedVue.vue';
import GroupedVueSource from '~/components/demos/list/GroupedVue.vue?raw';
import CardVue from '~/components/demos/list/CardVue.vue';
import CardVueSource from '~/components/demos/list/CardVue.vue?raw';
import SizesVue from '~/components/demos/list/SizesVue.vue';
import SizesVueSource from '~/components/demos/list/SizesVue.vue?raw';
import VariantsVue from '~/components/demos/list/VariantsVue.vue';
import VariantsVueSource from '~/components/demos/list/VariantsVue.vue?raw';

## 单选

`selectable="single"` 把列表变成单选控件，`v-model` 绑定 `string`。

<DemoFrame
  vueCode={`<script setup lang="ts">
import { ref } from 'vue';
import { CfList, type ListItem } from '@chufix-design/vue';

const value = ref<string | null>('alice');
const items: ListItem[] = [
  { key: 'alice', label: 'Alice Chen', description: '前端工程师 · 上海' },
  { key: 'bob',   label: 'Bob Wang',   description: '后端工程师 · 北京' },
  { key: 'carol', label: 'Carol Liu',  description: '设计师 · 深圳', disabled: true },
  { key: 'dave',  label: 'Dave Zhang', description: '产品经理 · 杭州' },
];
</script>

<template>
  <CfList v-model="value" selectable="single" :items="items" />
</template>`}
  reactCode={`import { useState } from 'react';
import { CfList, type ListItem } from '@chufix-design/react';

const items: ListItem[] = [/* ... */];

export default function Demo() {
  const [value, setValue] = useState<string>('alice');
  return (
    <CfList
      value={value}
      selectable="single"
      items={items}
      onChange={(v) => setValue(v as string)}
    />
  );
}`}

  vueSource={BasicVueSource}>
  <BasicVue client:load />
</DemoFrame>

## 多选 + 分组

每个 `ListItem` 可声明 `group`，相同 group 会自动聚合到一起，并显示分组标题。`selectable="multiple"` 时 `v-model` 绑定 `string[]`。

<DemoFrame
  vueCode={`<CfList v-model="value" selectable="multiple" :items="items" />`}
  reactCode={`<CfList value={value} selectable="multiple" items={items} onChange={setValue as any} />`}

  vueSource={GroupedVueSource}>
  <GroupedVue client:load />
</DemoFrame>

## 三种 variant

`variant` 控制视觉层次 —— `default`（紧凑，无分割线）/ `divided`（行间细分割线）/ `card`（每项独立卡片，自带间距）。

<DemoFrame
  vueCode={`<CfList variant="default" :items="items" />
<CfList variant="divided" :items="items" />
<CfList variant="card" :bordered="false" :items="items" />`}
  reactCode={`<CfList variant="default" items={items} />
<CfList variant="divided" items={items} />
<CfList variant="card" bordered={false} items={items} />`}

  vueSource={VariantsVueSource}>
  <VariantsVue client:load />
</DemoFrame>

## 三档尺寸

`size` 控制行高与字号 —— `sm` 适合长列表 / 紧凑后台、`md` 默认、`lg` 适合触摸友好的场景。

<DemoFrame
  vueCode={`<CfList size="sm" :items="items" />
<CfList size="md" :items="items" />
<CfList size="lg" :items="items" />`}
  reactCode={`<CfList size="sm" items={items} />
<CfList size="md" items={items} />
<CfList size="lg" items={items} />`}

  vueSource={SizesVueSource}>
  <SizesVue client:load />
</DemoFrame>

## 卡片化

`variant="card"` 把每一项渲染成独立卡片，自带间距与圆角，适合展示订单 / 通知等独立条目。

<DemoFrame
  vueCode={`<CfList :items="items" variant="card" :bordered="false" />`}
  reactCode={`<CfList items={items} variant="card" bordered={false} />`}

  vueSource={CardVueSource}>
  <CardVue client:load />
</DemoFrame>

## API

### Props

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `items` | `ListItem[]` | `[]` | 数据数组 |
| `modelValue` (Vue) / `value` (React) | `string \| string[] \| null` | `null` | 选中项；类型由 `selectable` 决定 |
| `selectable` | `'single' \| 'multiple' \| undefined` | `—` | 选择模式；省略则无选择行为 |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | 尺寸 |
| `variant` | `'default' \| 'divided' \| 'card'` | `'default'` | 视觉模式 |
| `bordered` | `boolean` | `true` | 整体描边 |
| `hoverable` | `boolean` | `true` | 鼠标悬停高亮 |
| `emptyText` | `string \| ReactNode` | `'暂无数据'` | 空状态文案 |

### ListItem

```ts
interface ListItem {
  key: string;
  label?: string;
  description?: string;
  leading?: string;   // 原始 SVG 字符串，通过 v-html 注入
  trailing?: string;  // 原始 SVG 字符串
  disabled?: boolean;
  group?: string;     // 分组 key，相同 group 自动聚合
  [extra: string]: unknown;
}
```

### Events

| Vue 事件 | React 回调 | 载荷类型 | 说明 |
|---|---|---|---|
| `update:modelValue` | `onChange` | `string \| string[] \| null` | 选中项变化时触发，类型与 `modelValue` 保持一致 |
| `select` | `onSelect` | `ListItem` | 点击单项时触发（包括非 selectable 模式），载荷是被点击的原始 item 对象 |

### Slots

| Slot | 作用域参数 | 说明 |
|---|---|---|
| `default` | `{ item: ListItem, selected: boolean }` | 完全自定义每行渲染（React 端：`renderItem={(item, selected) => ...}`） |
| `empty` | — | 自定义空状态（React 端：传 ReactNode 给 `emptyText`） |
