---
title: MetricCard 指标卡
description: 数值 + 单位 + 涨跌 + 内嵌 sparkline 趋势，支持展开 series 明细。
---

import DemoFrame from '~/components/DemoFrame.astro';
import BasicVue from '~/components/demos/metriccard/BasicVue.vue';
import BasicVueSource from '~/components/demos/metriccard/BasicVue.vue?raw';
import SeriesVue from '~/components/demos/metriccard/SeriesVue.vue';
import SeriesVueSource from '~/components/demos/metriccard/SeriesVue.vue?raw';

## 基础用法

数据通过 props 传入，纯 SVG 渲染，无第三方图表库依赖。
配色取自 `--viz-1..8` token，色盲友好。

<DemoFrame
  vueCode={`<CfMetricCard ... />`}
  reactCode={`<CfMetricCard ... />`}

  vueSource={BasicVueSource}>
  <BasicVue client:load />
</DemoFrame>


## 网格布局

配 `grid-template-columns: repeat(auto-fit, minmax(...))` 自适应密度。

import GridVue from '~/components/demos/metriccard/GridVue.vue';
import GridVueSource from '~/components/demos/metriccard/GridVue.vue?raw';

<DemoFrame
  vueCode={`<CfMetricCard label="…" value="…" :delta="…" :trend="[…]" />`}
  reactCode={`<CfMetricCard label="…" value="…" delta={…} trend={[…]} />`}

  vueSource={GridVueSource}>
  <GridVue client:load />
</DemoFrame>

## series 展开明细 + 行点击下钻

传入 `series` 数组,卡片右上角出现 chevron 按钮;点击后向下展开渠道 / 组件 / 来源等明细行,每行可带自己的 sparkline、delta 和色板点。可用 `defaultExpanded` 控制初始态,或 `expanded` + `@update:expanded` 做受控开关。

每一行都是 `role="button"` + `tabindex=0`,点击或键盘 Enter / Space 触发 `@series-select` (payload `{ index, item }`),最适合"点 KPI 行 → 跳到下钻页面 / 打开 Drawer"的联动。

<DemoFrame
  vueCode={`<CfMetricCard label="本月营收" value="12,060" :delta="6.4" :series="channels" />`}
  reactCode={`<CfMetricCard label="本月营收" value="12,060" delta={6.4} series={channels} />`}
  vueSource={SeriesVueSource}>
  <SeriesVue client:load />
</DemoFrame>

## API

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `label` | `string` | `—` | 顶部小标签 |
| `value` | `string \| number` | `—` | 主数值 |
| `prefix` | `string` | `—` | 数值前缀（如货币符号 `$`） |
| `suffix` | `string` | `—` | 数值后缀（与 `unit` 同义，dashboard 风格） |
| `unit` | `string` | `—` | 单位（数值后） |
| `hint` | `string` | `—` | 数值下方的辅助说明小字 |
| `delta` | `number` | `—` | 涨跌百分比，正负染色 |
| `trend` | `number[] \| 'up' \| 'down' \| 'flat'` | `—` | 内嵌 Sparkline 数据；或传 `'up'`/`'down'`/`'flat'` 显示对应趋势图标 |
| `deltaFn` | `(delta: number) => string` | `+N%` | 自定义 `delta` 文案格式化（如绝对值、保留小数位等） |
| `series` | `MetricSeriesItem[]` | `—` | 展开后展示的明细行 |
| `expandable` | `boolean` | `true` | 有 series 时是否显示 chevron |
| `defaultExpanded` | `boolean` | `false` | 非受控初始态 |
| `expanded` | `boolean` | `—` | 受控展开标志，配合 `@update:expanded` (Vue) / `onExpandedChange` (React) |
| `ariaLabel` | `string` | `—` | 透传给根元素的 `aria-label` |

### 事件

| Vue 事件 | React 回调 | payload | 说明 |
| --- | --- | --- | --- |
| `update:expanded` | `onExpandedChange` | `boolean` | 受控开关 |
| `expand` | — | `{ expanded }` | 展开 / 收起触发 |
| `series-select` | `onSeriesSelect` | `{ index, item }` | 明细行点击 / 键盘 Enter / Space |

### 类型

```ts
interface MetricSeriesItem {
  label: string;
  value: string | number;
  prefix?: string;
  suffix?: string;
  delta?: number;
  /** 内嵌迷你 sparkline。 */
  trend?: number[];
  /** 行首色板点的颜色覆盖。 */
  color?: string;
}
```
