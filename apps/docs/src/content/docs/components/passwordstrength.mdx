---
title: PasswordStrength 密码强度
description: 密码输入 + 4 段强度条 + 实时需求清单 + 显隐切换。
---

import DemoFrame from '~/components/DemoFrame.astro';
import BasicVue from '~/components/demos/passwordstrength/BasicVue.vue';
import BasicVueSource from '~/components/demos/passwordstrength/BasicVue.vue?raw';

## 基础用法

默认 4 条规则（8+ 字符 / 大写 / 数字 / 符号），用户可通过 `requirements` 自定义。

<DemoFrame
  vueCode={`<CfPasswordStrength v-model="pwd" />`}
  reactCode={`<CfPasswordStrength value={pwd} onChange={setPwd} />`}

  vueSource={BasicVueSource}>
  <BasicVue client:load />
</DemoFrame>


## 自定义规则

可传 `requirements` 替换默认 4 条规则，每条 `{ label, test }`。

import CustomRulesVue from '~/components/demos/passwordstrength/CustomRulesVue.vue';
import CustomRulesVueSource from '~/components/demos/passwordstrength/CustomRulesVue.vue?raw';

<DemoFrame
  vueCode={`<CfPasswordStrength v-model="pwd" :requirements="rules" />`}
  reactCode={`<CfPasswordStrength value={pwd} onChange={setPwd} requirements={rules} />`}

  vueSource={CustomRulesVueSource}>
  <CustomRulesVue client:load />
</DemoFrame>

## API

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `modelValue` / `v-model` (Vue), `value/onChange` (React) | `string` | `''` | 密码值 |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | |
| `placeholder` | `string` | `'请输入密码'` | |
| `disabled` | `boolean` | `false` | |
| `showToggle` | `boolean` | `true` | 显示眼睛切换显隐 |
| `requirements` | `PasswordRequirement[]` | `defaultPasswordRequirements` | `{ label, test }[]` |
