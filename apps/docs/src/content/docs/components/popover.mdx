---
title: Popover 弹出层
description: Tooltip 的可交互变体 —— 支持任意富内容、表单、按钮，自动避让屏幕边缘。
---

import DemoFrame from '~/components/DemoFrame.astro';
import BasicVue from '~/components/demos/popover/BasicVue.vue';
import BasicVueSource from '~/components/demos/popover/BasicVue.vue?raw';
import TriggersVue from '~/components/demos/popover/TriggersVue.vue';
import TriggersVueSource from '~/components/demos/popover/TriggersVue.vue?raw';
import PlacementVue from '~/components/demos/popover/PlacementVue.vue';
import PlacementVueSource from '~/components/demos/popover/PlacementVue.vue?raw';
import WidthVue from '~/components/demos/popover/WidthVue.vue';
import WidthVueSource from '~/components/demos/popover/WidthVue.vue?raw';
import DisabledVue from '~/components/demos/popover/DisabledVue.vue';
import DisabledVueSource from '~/components/demos/popover/DisabledVue.vue?raw';
import CloseBehaviorVue from '~/components/demos/popover/CloseBehaviorVue.vue';
import CloseBehaviorVueSource from '~/components/demos/popover/CloseBehaviorVue.vue?raw';

## 基础用法

与 Tooltip 不同，Popover 内部支持任意可交互内容（输入、按钮、列表）。点击外部或按 Esc 关闭，鼠标移入悬停 Popover 不会关闭。

<DemoFrame
  vueCode={`<CfPopover placement="bottom">
  <CfButton variant="secondary">点击展开</CfButton>
  <template #content>
    <strong>快速操作</strong>
    <CfInput placeholder="搜索…" size="sm" />
    <CfButton size="sm">应用</CfButton>
  </template>
</CfPopover>`}
  reactCode={`<CfPopover
  placement="bottom"
  content={
    <div>
      <strong>快速操作</strong>
      <CfInput placeholder="搜索…" size="sm" />
      <CfButton size="sm">应用</CfButton>
    </div>
  }
>
  <CfButton variant="secondary">点击展开</CfButton>
</CfPopover>`}

  vueSource={BasicVueSource}>
  <BasicVue client:only="vue" />
</DemoFrame>

## 触发方式

`trigger` 决定如何打开 popover：

- `click`（默认）—— 点击触发器打开，再点击或点外部关闭
- `hover` —— 悬停打开，移开延时关闭
- `manual` —— 完全由父组件通过 `open` 控制（受控模式）

<DemoFrame
  vueCode={`<CfPopover trigger="click"><CfButton>click</CfButton><template #content>…</template></CfPopover>
<CfPopover trigger="hover"><CfButton>hover</CfButton><template #content>…</template></CfPopover>
<CfPopover trigger="manual" :open="manualOpen" @update:open="(v) => manualOpen = v">
  <CfButton @click="manualOpen = !manualOpen">manual</CfButton>
  <template #content>…</template>
</CfPopover>`}
  reactCode={`<CfPopover trigger="click" content={...}><CfButton>click</CfButton></CfPopover>
<CfPopover trigger="hover" content={...}><CfButton>hover</CfButton></CfPopover>
<CfPopover trigger="manual" open={open} onOpenChange={setOpen} content={...}>
  <CfButton onClick={() => setOpen(!open)}>manual</CfButton>
</CfPopover>`}

  vueSource={TriggersVueSource}>
  <TriggersVue client:only="vue" />
</DemoFrame>

## 4 种 placement

`placement` —— `top` / `bottom`（默认）/ `left` / `right`，空间不够时自动翻转到对侧。

<DemoFrame
  vueCode={`<CfPopover placement="top">…</CfPopover>
<CfPopover placement="bottom">…</CfPopover>
<CfPopover placement="left">…</CfPopover>
<CfPopover placement="right">…</CfPopover>`}
  reactCode={`<CfPopover placement="top">…</CfPopover>
<CfPopover placement="bottom">…</CfPopover>
<CfPopover placement="left">…</CfPopover>
<CfPopover placement="right">…</CfPopover>`}

  vueSource={PlacementVueSource}>
  <PlacementVue client:only="vue" />
</DemoFrame>

## 固定宽度

`width` 给 popover 一个固定宽度（数字或 CSS 字符串）；省略则按内容自适应。需要多列内容或精准排版时用。

<DemoFrame
  vueCode={`<CfPopover placement="bottom"><CfButton>自适应</CfButton></CfPopover>
<CfPopover placement="bottom" :width="320"><CfButton>固定 320</CfButton></CfPopover>`}
  reactCode={`<CfPopover placement="bottom" content={...}><CfButton>自适应</CfButton></CfPopover>
<CfPopover placement="bottom" width={320} content={...}><CfButton>固定 320</CfButton></CfPopover>`}

  vueSource={WidthVueSource}>
  <WidthVue client:only="vue" />
</DemoFrame>

## 控制关闭路径

默认 popover 在三种情形会关闭：点击外部、按 Esc、再次点击触发器。把 `closeOnOutside` / `closeOnEsc` 单独置 false 可锁死对应路径 —— 常用于内嵌表单 popover，避免误触关闭丢失输入。

<DemoFrame
  vueCode={`<CfPopover :close-on-outside="false">
  <CfButton variant="secondary">外部点击不关</CfButton>
  <template #content>仅 Esc 或再点触发器才能关</template>
</CfPopover>

<CfPopover :close-on-esc="false">
  <CfButton variant="secondary">Esc 不关</CfButton>
  <template #content>仅外部点击或再点触发器才能关</template>
</CfPopover>`}
  reactCode={`<CfPopover closeOnOutside={false} content={...}>
  <CfButton variant="secondary">外部点击不关</CfButton>
</CfPopover>

<CfPopover closeOnEsc={false} content={...}>
  <CfButton variant="secondary">Esc 不关</CfButton>
</CfPopover>`}

  vueSource={CloseBehaviorVueSource}>
  <CloseBehaviorVue client:only="vue" />
</DemoFrame>

## 禁用 popover

`disabled` 让触发器失去任何打开 popover 的能力 —— 触发器本身依然渲染、可点击，但不会弹出浮层。常用于权限或条件互斥的场景。

<DemoFrame
  vueCode={`<CfPopover :disabled="disabled">
  <CfButton variant="secondary">点击展开</CfButton>
  <template #content>内容…</template>
</CfPopover>

<CfButton size="sm" variant="ghost" @click="disabled = !disabled">
  {{ disabled ? '启用' : '禁用' }} popover
</CfButton>`}
  reactCode={`<CfPopover disabled={disabled} content={...}>
  <CfButton variant="secondary">点击展开</CfButton>
</CfPopover>

<CfButton size="sm" variant="ghost" onClick={() => setDisabled(!disabled)}>
  {disabled ? '启用' : '禁用'} popover
</CfButton>`}

  vueSource={DisabledVueSource}>
  <DisabledVue client:only="vue" />
</DemoFrame>

## API · Props

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `open` | `boolean` | `—` | 受控时使用，配合 `update:open` / `onOpenChange` |
| `placement` | `'top' \| 'bottom' \| 'left' \| 'right'` | `'bottom'` | 首选位置；空间不够时自动翻转到对侧 |
| `trigger` | `'click' \| 'hover' \| 'manual'` | `'click'` | 触发方式 |
| `offset` | `number` | `8` | 与触发元素的间距（px） |
| `closeOnOutside` | `boolean` | `true` | 点击外部时关闭（`manual` 时忽略） |
| `closeOnEsc` | `boolean` | `true` | Esc 关闭 |
| `width` | `number \| string` | `—` | 固定宽度，省略则自适应内容 |
| `disabled` | `boolean` | `false` | 禁用触发 |

## API · Slots / 子节点

| 名称 | 说明 |
|---|---|
| `default` (children) | 触发元素 |
| `content` | Popover 面板内容（React 用 `content` prop） |
