---
title: ScatterPlot 散点图
description: 二维散点，可按 group 分色。
---

import DemoFrame from '~/components/DemoFrame.astro';
import BasicVue from '~/components/demos/scatterplot/BasicVue.vue';
import BasicVueSource from '~/components/demos/scatterplot/BasicVue.vue?raw';

## 基础用法

数据通过 props 传入，纯 SVG 渲染，无第三方图表库依赖。
配色取自 `--viz-1..8` token，色盲友好。

<DemoFrame
  vueCode={`<CfScatterPlot ... />`}
  reactCode={`<CfScatterPlot ... />`}

  vueSource={BasicVueSource}>
  <BasicVue client:load />
</DemoFrame>


## 多 group 聚类

点的 `group` 字段决定颜色，未传则归到 `default`。

import GroupsVue from '~/components/demos/scatterplot/GroupsVue.vue';
import GroupsVueSource from '~/components/demos/scatterplot/GroupsVue.vue?raw';

<DemoFrame
  vueCode={`<CfScatterPlot :data="points" />`}
  reactCode={`<CfScatterPlot data={points} />`}

  vueSource={GroupsVueSource}>
  <GroupsVue client:load />
</DemoFrame>

## API

### Props

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `data` | `ScatterPoint[]` | `—` | `{ x, y, r?, group?, label? }[]` |
| `width` | `number` | `480` | SVG 宽度 |
| `height` | `number` | `240` | SVG 高度 |
| `showGrid` | `boolean` | `true` | 显示网格 |
| `showTooltip` | `boolean` | `true` | 启用 hover tooltip |
| `tooltipFormatter` | `(payload: ScatterPlotInteractionPayload) => string` | — | 完整 tooltip HTML 自渲染 |
| `ariaLabel` | `string` | — | 透传给根 `<svg>` 的 `aria-label` |

### Events

| Vue 事件 | React 回调 | 载荷类型 | 说明 |
|---|---|---|---|
| `item-enter` | `onItemEnter` | `ScatterPlotInteractionPayload` | 鼠标进入某点时触发 |
| `item-leave` | `onItemLeave` | `ScatterPlotInteractionPayload` | 鼠标离开时触发 |

### 类型

```ts
interface ScatterPoint {
  x: number;
  y: number;
  r?: number;       // 气泡半径（默认按数据自适应）
  group?: string;   // 用于按组分色
  label?: string;   // 单点标签
}

interface ScatterPlotInteractionPayload {
  point: ScatterPoint;
  dataIndex: number;
  groupIndex: number;
  nativeEvent?: MouseEvent;
}
```
