---
title: Stat / KPI 关键指标
description: 数字 + 标签 + 趋势的指标卡片，适合 dashboard 顶部的指标摘要。
---

import DemoFrame from '~/components/DemoFrame.astro';
import BasicVue from '~/components/demos/stat/BasicVue.vue';
import VariantsVue from '~/components/demos/stat/VariantsVue.vue';

## 基础用法

`label` + `value` 是最低配置；加 `trend` 显示带方向色（up / down / flat）的变化指标，`prefix` / `suffix` 给数字加前后缀。

<DemoFrame
  vueCode={`<CfStat
  label="今日访问"
  :value="12384"
  :trend="{ delta: '+12.4%', label: '环比昨日' }"
  variant="outlined"
/>
<CfStat
  label="转化率"
  value="3.2"
  suffix="%"
  :trend="{ delta: '-0.4%', label: '环比昨日' }"
  variant="outlined"
/>
<CfStat
  label="收入"
  :value="84520"
  prefix="¥"
  :trend="{ delta: 0, label: '与昨日持平' }"
  variant="outlined"
/>`}
  reactCode={`<CfStat
  label="今日访问"
  value={12384}
  trend={{ delta: '+12.4%', label: '环比昨日' }}
  variant="outlined"
/>
<CfStat
  label="转化率"
  value="3.2"
  suffix="%"
  trend={{ delta: '-0.4%', label: '环比昨日' }}
  variant="outlined"
/>
<CfStat
  label="收入"
  value={84520}
  prefix="¥"
  trend={{ delta: 0, label: '与昨日持平' }}
  variant="outlined"
/>`}
>
  <BasicVue client:load />
</DemoFrame>

## 三种变体

`variant` 控制背景与边框：`default` 透明、`outlined` 描边、`soft` 浅灰底。

<DemoFrame
  vueCode={`<CfStat label="default" :value="42" />
<CfStat label="outlined" :value="42" variant="outlined" />
<CfStat label="soft" :value="42" variant="soft" />`}
  reactCode={`<CfStat label="default" value={42} />
<CfStat label="outlined" value={42} variant="outlined" />
<CfStat label="soft" value={42} variant="soft" />`}
>
  <VariantsVue client:load />
</DemoFrame>

## API

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `label` | `string \| ReactNode` | `—` | 顶部小字标签 |
| `value` | `string \| number \| ReactNode` | `—` | 主数字 |
| `prefix` / `suffix` | `string \| ReactNode` | `—` | 数字前后小字 |
| `hint` | `string \| ReactNode` | `—` | 底部辅助说明 |
| `trend` | `StatTrend` | `—` | `{ delta, direction?, label? }`，方向不传时按数值符号自动推断 |
| `variant` | `'default' \| 'outlined' \| 'soft'` | `'default'` | 视觉模式 |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | 数字字号 |
| `loading` | `boolean` | `false` | 加载态，数字位显示 `—` |

Slot（Vue）/ prop（React）：`leading` / `trailing`（label 行两端）、`footer`（底部追加）。
