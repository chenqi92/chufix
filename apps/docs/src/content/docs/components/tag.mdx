---
title: Tag 标签
description: 标签 —— 3 种 variant、6 种 tone、3 种尺寸，可关闭、可圆角。
---

import DemoFrame from '~/components/DemoFrame.astro';
import BasicVue from '~/components/demos/tag/BasicVue.vue';
import BasicVueSource from '~/components/demos/tag/BasicVue.vue?raw';
import VariantsVue from '~/components/demos/tag/VariantsVue.vue';
import VariantsVueSource from '~/components/demos/tag/VariantsVue.vue?raw';
import ClosableVue from '~/components/demos/tag/ClosableVue.vue';
import ClosableVueSource from '~/components/demos/tag/ClosableVue.vue?raw';
import LeadingVue from '~/components/demos/tag/LeadingVue.vue';
import LeadingVueSource from '~/components/demos/tag/LeadingVue.vue?raw';

## 基础用法

`tone` 控制语义色，默认 `neutral`（灰色）。其他选项：`primary` / `success` / `warning` / `danger` / `info`。

<DemoFrame
  vueCode={`<script setup lang="ts">
import { CfTag } from '@chufix-design/vue';
</script>

<template>
  <CfTag>Default</CfTag>
  <CfTag tone="primary">Primary</CfTag>
  <CfTag tone="success">Success</CfTag>
  <CfTag tone="warning">Warning</CfTag>
  <CfTag tone="danger">Danger</CfTag>
  <CfTag tone="info">Info</CfTag>
</template>`}
  reactCode={`import { CfTag } from '@chufix-design/react';

export default function Demo() {
  return (
    <>
      <CfTag>Default</CfTag>
      <CfTag tone="primary">Primary</CfTag>
      <CfTag tone="success">Success</CfTag>
      <CfTag tone="warning">Warning</CfTag>
      <CfTag tone="danger">Danger</CfTag>
      <CfTag tone="info">Info</CfTag>
    </>
  );
}`}

  vueSource={BasicVueSource}>
  <BasicVue client:load />
</DemoFrame>

## 变体、尺寸与圆角

`variant` 与 `tone` 正交：solid / soft（默认）/ outline。`rounded` 切换到 pill 圆角。

<DemoFrame
  vueCode={`<CfTag variant="solid" tone="primary">solid</CfTag>
<CfTag variant="soft" tone="primary">soft</CfTag>
<CfTag variant="outline" tone="primary">outline</CfTag>

<CfTag size="sm" tone="success">sm</CfTag>
<CfTag size="md" tone="success">md</CfTag>
<CfTag size="lg" tone="success">lg</CfTag>
<CfTag tone="success" rounded>rounded</CfTag>`}
  reactCode={`<CfTag variant="solid" tone="primary">solid</CfTag>
<CfTag variant="soft" tone="primary">soft</CfTag>
<CfTag variant="outline" tone="primary">outline</CfTag>

<CfTag size="sm" tone="success">sm</CfTag>
<CfTag size="md" tone="success">md</CfTag>
<CfTag size="lg" tone="success">lg</CfTag>
<CfTag tone="success" rounded>rounded</CfTag>`}

  vueSource={VariantsVueSource}>
  <VariantsVue client:load />
</DemoFrame>

## 可关闭

打开 `closable` 显示 × 按钮，点击触发 `close` / `onClose`。

<DemoFrame
  vueCode={`<CfTag
  v-for="t in tags"
  :key="t"
  tone="primary"
  closable
  @close="tags = tags.filter(x => x !== t)"
>{{ t }}</CfTag>`}
  reactCode={`{tags.map((t) => (
  <CfTag
    key={t}
    tone="primary"
    closable
    onClose={() => setTags((xs) => xs.filter((x) => x !== t))}
  >{t}</CfTag>
))}`}

  vueSource={ClosableVueSource}>
  <ClosableVue client:load />
</DemoFrame>

## 前置图标 / 状态点

`leading` 具名插槽（React 端：`leading` prop）渲染在文本左侧。常见做法是放一个小 SVG 图标或纯色圆点，用来表示状态（已发布 / 草稿 / 已归档）或区别同色 tag。

<DemoFrame
  vueCode={`<CfTag tone="success">
  <template #leading>
    <svg viewBox="0 0 16 16" width="10" height="10" ...>
      <path d="M3 8.5l3 3L13 5" />
    </svg>
  </template>
  已发布
</CfTag>

<CfTag tone="warning">
  <template #leading><span class="dot" /></template>
  草稿
</CfTag>`}
  reactCode={`<CfTag tone="success" leading={<CheckIcon />}>已发布</CfTag>
<CfTag tone="warning" leading={<span className="dot" />}>草稿</CfTag>`}

  vueSource={LeadingVueSource}>
  <LeadingVue client:load />
</DemoFrame>

## API

### Props

| Prop       | 类型                                                                     | 默认值      | 说明                          |
| ---------- | ------------------------------------------------------------------------ | ----------- | ----------------------------- |
| `variant`  | `'solid' \| 'soft' \| 'outline'`                                         | `'soft'`    | 视觉变体                      |
| `tone`     | `'neutral' \| 'primary' \| 'success' \| 'warning' \| 'danger' \| 'info'` | `'neutral'` | 语义色                        |
| `size`     | `'sm' \| 'md' \| 'lg'`                                                   | `'md'`      | 高度与字号                    |
| `rounded`  | `boolean`                                                                | `false`     | 圆角到 pill                   |
| `closable` | `boolean`                                                                | `false`     | 显示 × 关闭按钮               |

### Events

| Vue 事件 | React 回调 | 载荷类型 | 说明 |
|---|---|---|---|
| `close` | `onClose` | `MouseEvent` | 点击 × 关闭按钮时触发 |

### Slots / Children

| Slot | 作用域参数 | 说明 |
|---|---|---|
| `default` | — | tag 文本（React 端：`children`） |
| `leading` | — | 文本左侧的图标 / 状态点（React 端：`leading` prop） |
