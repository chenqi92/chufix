---
title: TagInput 标签输入
description: 多标签输入控件，键入回车追加，退格删除，支持粘贴批量导入。
---

import DemoFrame from '~/components/DemoFrame.astro';
import BasicVue from '~/components/demos/taginput/BasicVue.vue';
import BasicVueSource from '~/components/demos/taginput/BasicVue.vue?raw';
import TonesVue from '~/components/demos/taginput/TonesVue.vue';
import TonesVueSource from '~/components/demos/taginput/TonesVue.vue?raw';
import SeparatorsVue from '~/components/demos/taginput/SeparatorsVue.vue';
import SeparatorsVueSource from '~/components/demos/taginput/SeparatorsVue.vue?raw';
import MaxValidateVue from '~/components/demos/taginput/MaxValidateVue.vue';
import MaxValidateVueSource from '~/components/demos/taginput/MaxValidateVue.vue?raw';
import SizesVue from '~/components/demos/taginput/SizesVue.vue';
import SizesVueSource from '~/components/demos/taginput/SizesVue.vue?raw';
import VariantsVue from '~/components/demos/taginput/VariantsVue.vue';
import VariantsVueSource from '~/components/demos/taginput/VariantsVue.vue?raw';

## 基础用法

`v-model` 绑定一个 `string[]`。键入文字后按 `Enter` 追加；输入框为空时按 `Backspace` 删除最后一个标签；点击标签上的 × 也可移除。

<DemoFrame
  vueCode={`<script setup lang="ts">
import { ref } from 'vue';
import { CfTagInput } from '@chufix-design/vue';

const tags = ref<string[]>(['vue', 'react']);
</script>

<template>
  <CfTagInput v-model="tags" placeholder="输入后按回车添加" />
</template>`}
  reactCode={`import { useState } from 'react';
import { CfTagInput } from '@chufix-design/react';

export default function Demo() {
  const [tags, setTags] = useState<string[]>(['vue', 'react']);
  return <CfTagInput value={tags} placeholder="输入后按回车添加" onChange={setTags} />;
}`}

  vueSource={BasicVueSource}>
  <BasicVue client:load />
</DemoFrame>

## 色调

`tone` 控制标签底色，与 Tag 组件的语义色一一对应。

<DemoFrame
  vueCode={`<CfTagInput v-model="a" tone="neutral" />
<CfTagInput v-model="b" tone="accent" />
<CfTagInput v-model="c" tone="success" />
<CfTagInput v-model="d" tone="warning" />
<CfTagInput v-model="e" tone="danger" />`}
  reactCode={`<CfTagInput value={a} tone="neutral" onChange={setA} />
<CfTagInput value={b} tone="accent"  onChange={setB} />
<CfTagInput value={c} tone="success" onChange={setC} />
<CfTagInput value={d} tone="warning" onChange={setD} />
<CfTagInput value={e} tone="danger"  onChange={setE} />`}

  vueSource={TonesVueSource}>
  <TonesVue client:load />
</DemoFrame>

## 三档尺寸

`size` 与 Input、Select 保持一致 —— `sm` / `md` / `lg`。

<DemoFrame
  vueCode={`<CfTagInput v-model="a" size="sm" />
<CfTagInput v-model="b" size="md" />
<CfTagInput v-model="c" size="lg" />`}
  reactCode={`<CfTagInput value={a} onChange={setA} size="sm" />
<CfTagInput value={b} onChange={setB} size="md" />
<CfTagInput value={c} onChange={setC} size="lg" />`}

  vueSource={SizesVueSource}>
  <SizesVue client:load />
</DemoFrame>

## 视觉变体

`variant` —— `outline`（默认，带描边）/ `filled`（实底，密集表单常用）/ `ghost`（透明，悬停描边）。`error` 切到红色边框，常配合表单校验。

<DemoFrame
  vueCode={`<CfTagInput v-model="a" variant="outline" />
<CfTagInput v-model="b" variant="filled" />
<CfTagInput v-model="c" variant="ghost" />
<CfTagInput v-model="d" error placeholder="error 态" />`}
  reactCode={`<CfTagInput value={a} onChange={setA} variant="outline" />
<CfTagInput value={b} onChange={setB} variant="filled" />
<CfTagInput value={c} onChange={setC} variant="ghost" />
<CfTagInput value={d} onChange={setD} error placeholder="error 态" />`}

  vueSource={VariantsVueSource}>
  <VariantsVue client:load />
</DemoFrame>

## 自定义分隔符

`separators` 是一个键名数组，键入这些键就把当前草稿提交为标签。逗号 / 空格 / Tab 都可以加进去。粘贴时会按 `,` `\\n` `\\t` 自动拆分。

<DemoFrame
  vueCode={`<CfTagInput
  v-model="tags"
  :separators="['Enter', ',', ' ']"
  placeholder="回车 / 逗号 / 空格 都能分隔"
/>`}
  reactCode={`<CfTagInput
  value={tags}
  separators={['Enter', ',', ' ']}
  placeholder="回车 / 逗号 / 空格 都能分隔"
  onChange={setTags}
/>`}

  vueSource={SeparatorsVueSource}>
  <SeparatorsVue client:load />
</DemoFrame>

## 数量限制 / 校验

`max` 限制总数，`validate` 是一个返回 `boolean` 的函数；不通过的输入会被默默丢弃。

<DemoFrame
  vueCode={`<script setup lang="ts">
import { ref } from 'vue';
import { CfTagInput } from '@chufix-design/vue';

const tags = ref<string[]>([]);
const isEmail = (t: string) => /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(t);
</script>

<template>
  <CfTagInput
    v-model="tags"
    :max="5"
    :validate="isEmail"
    :separators="['Enter', ',', ' ']"
    placeholder="最多 5 个邮箱地址"
  />
</template>`}
  reactCode={`import { useState } from 'react';
import { CfTagInput } from '@chufix-design/react';

const isEmail = (t: string) => /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(t);

export default function Demo() {
  const [tags, setTags] = useState<string[]>([]);
  return (
    <CfTagInput
      value={tags}
      max={5}
      validate={isEmail}
      separators={['Enter', ',', ' ']}
      placeholder="最多 5 个邮箱地址"
      onChange={setTags}
    />
  );
}`}

  vueSource={MaxValidateVueSource}>
  <MaxValidateVue client:load />
</DemoFrame>

## API

### Props

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `modelValue` (Vue) / `value` (React) | `string[]` | `[]` | 当前标签数组 |
| `placeholder` | `string` | `'输入后回车添加'` | 占位文案，仅在空状态显示 |
| `variant` | `'outline' \| 'filled' \| 'ghost'` | `'outline'` | 视觉模式 |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | 尺寸 |
| `tone` | `'neutral' \| 'accent' \| 'success' \| 'warning' \| 'danger'` | `'neutral'` | 标签底色 |
| `disabled` | `boolean` | `false` | 禁用 |
| `error` | `boolean` | `false` | 错误态 |
| `max` | `number` | `—` | 标签数量上限 |
| `separators` | `string[]` | `['Enter']` | 触发提交的键，可包含 `'Enter'` `','` `' '` `'Tab'` 或单字符 |
| `unique` | `boolean` | `true` | 是否去重，重复的输入会被忽略 |
| `trim` | `boolean` | `true` | 提交前是否 trim |
| `validate` | `(tag: string) => boolean` | `—` | 返回 `false` 的输入会被丢弃 |
| `name` | `string` | — | 透传给隐式表单字段，用于原生表单提交 |
| `id` | `string` | — | 根元素 id，便于外部 `<label for>` 关联 |

### Events

| Vue 事件 | React 回调 | 载荷类型 | 说明 |
|---|---|---|---|
| `update:modelValue` | `onChange` | `string[]` | 标签数组发生变化时（新增或删除）触发 |
| `add` | `onAdd` | `string` | 新增单个标签时触发，载荷是被添加的字符串 |
| `remove` | `onRemove` | `(tag: string, index: number)` | 删除单个标签时触发；提供原值与位置索引 |
