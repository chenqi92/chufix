---
title: Timeline 时间轴
description: 顺序事件流，每条带圆点 / 连线 / 时间戳，常用于变更日志、流水审计、版本发布。
---

import DemoFrame from '~/components/DemoFrame.astro';
import BasicVue from '~/components/demos/timeline/BasicVue.vue';
import BasicVueSource from '~/components/demos/timeline/BasicVue.vue?raw';

## 基础用法

`items` 数组每一项为一个节点，`color` 控制圆点颜色（`primary` / `success` / `warning` / `danger` / `neutral`）。

<DemoFrame
  vueCode={`<CfTimeline :items="items" />`}
  reactCode={`<CfTimeline items={items} />`}

  vueSource={BasicVueSource}>
  <BasicVue client:load />
</DemoFrame>

## API

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `items` | `TimelineItem[]` | `—` | 节点列表 |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | 节点字号与点的尺寸 |
| `mode` | `'left' \| 'right' \| 'alternate'` | `'left'` | 内容相对连线的位置 |
| `reverse` | `boolean` | `false` | 反序渲染（最新在最上） |
| `className` | `string` | `—` | 透传到根元素的额外类名 |

### TimelineItem

| 字段 | 类型 | 说明 |
|---|---|---|
| `key` | `string \| number` | 唯一标识，用于 v-for / map key |
| `title` | `string` | 主标题 |
| `content` | `string` | 详细描述 |
| `time` | `string` | 时间戳文本 |
| `color` | `'primary' \| 'success' \| 'warning' \| 'danger' \| 'neutral'` | 圆点颜色 |
| `icon` | `unknown` | 替换圆点的自定义 icon 节点（Vue VNode 或 React ReactNode） |
