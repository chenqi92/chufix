---
title: TimePicker 时间选择器
description: 触发器 + 三列滚动面板的时间选择器，支持时分秒，可配 DatePicker 用于"日期 + 时间"场景。
---

import DemoFrame from '~/components/DemoFrame.astro';
import BasicVue from '~/components/demos/timepicker/BasicVue.vue';
import BasicVueSource from '~/components/demos/timepicker/BasicVue.vue?raw';
import SizesVue from '~/components/demos/timepicker/SizesVue.vue';
import SizesVueSource from '~/components/demos/timepicker/SizesVue.vue?raw';
import StatesVue from '~/components/demos/timepicker/StatesVue.vue';
import StatesVueSource from '~/components/demos/timepicker/StatesVue.vue?raw';
import WithDatePickerVue from '~/components/demos/timepicker/WithDatePickerVue.vue';
import WithDatePickerVueSource from '~/components/demos/timepicker/WithDatePickerVue.vue?raw';

## 基础用法

`v-model` / `value` 是字符串，格式 `HH:mm` 或 `HH:mm:ss`（开启 `showSeconds` 时）。`clearable` 显示清除按钮。

<DemoFrame
  vueCode={`<CfTimePicker v-model="t1" clearable />
<CfTimePicker v-model="t2" show-seconds clearable />`}
  reactCode={`<CfTimePicker value={t1} onChange={setT1} clearable />
<CfTimePicker value={t2} onChange={setT2} showSeconds clearable />`}

  vueSource={BasicVueSource}>
  <BasicVue client:load />
</DemoFrame>

## 三档尺寸

`size` 与 Input、DatePicker 保持一致 —— `sm` 紧凑（表格内嵌选择常用）/ `md` 默认 / `lg` 强调。

<DemoFrame
  vueCode={`<CfTimePicker v-model="a" size="sm" />
<CfTimePicker v-model="b" size="md" />
<CfTimePicker v-model="c" size="lg" />`}
  reactCode={`<CfTimePicker value={a} onChange={setA} size="sm" />
<CfTimePicker value={b} onChange={setB} size="md" />
<CfTimePicker value={c} onChange={setC} size="lg" />`}

  vueSource={SizesVueSource}>
  <SizesVue client:load />
</DemoFrame>

## 禁用状态

`disabled` 让触发器变灰且不响应点击；已选中的值仍然展示。

<DemoFrame
  vueCode={`<CfTimePicker v-model="a" disabled />
<CfTimePicker v-model="b" placeholder="无值" disabled />`}
  reactCode={`<CfTimePicker value={a} onChange={setA} disabled />
<CfTimePicker value={b} onChange={setB} placeholder="无值" disabled />`}

  vueSource={StatesVueSource}>
  <StatesVue client:load />
</DemoFrame>

## 与 DatePicker 组合 (日期 + 时间)

TimePicker 不内置"日期 + 时间"二合一控件 —— 推荐做法是两个组件并排，在父组件里把 `date` 与 `time` 字符串拼成最终值。

<DemoFrame
  vueCode={`<script setup>
const date = ref('2026-05-09');
const time = ref('14:30');
const combined = computed(() => date.value && \`\${date.value} \${time.value ?? ''}\`);
</script>

<CfDatePicker v-model="date" />
<CfTimePicker v-model="time" show-seconds />
<!-- combined = '2026-05-09 14:30' -->`}
  reactCode={`const [date, setDate] = useState('2026-05-09');
const [time, setTime] = useState('14:30');
const combined = date && \`\${date}\${time ? ' ' + time : ''}\`;

<CfDatePicker value={date} onChange={setDate} />
<CfTimePicker value={time} onChange={setTime} showSeconds />`}

  vueSource={WithDatePickerVueSource}>
  <WithDatePickerVue client:load />
</DemoFrame>

## API

### Props

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `modelValue` (Vue) / `value` (React) | `string \| null` | `—` | 受控值，格式 `HH:mm` 或 `HH:mm:ss` |
| `defaultValue` | `string \| null` | `null` | 非受控初始值 |
| `placeholder` | `string` | `'请选择时间'` | 未选中时占位文案 |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | 触发器尺寸 |
| `disabled` | `boolean` | `false` | 禁用 |
| `clearable` | `boolean` | `false` | 显示清除按钮 |
| `showSeconds` | `boolean` | `false` | 显示秒列，输出格式变为 `HH:mm:ss` |
| `className` | `string` | — | 透传到根元素的额外类名 |

### Events

| Vue 事件 | React 回调 | 载荷类型 | 说明 |
|---|---|---|---|
| `update:modelValue` | `onChange` | `string \| null` | 时间变化时触发；载荷是 `HH:mm` 或 `HH:mm:ss` 字符串，清空时为 `null` |
| `change` | `onChange` | `string \| null` | 与 `update:modelValue` 同步触发，载荷相同 |
