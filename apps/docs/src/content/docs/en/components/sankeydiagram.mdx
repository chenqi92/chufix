---
title: Sankey diagram
description: Weighted flow diagram with nodes and links — node height equals flow.
---

import DemoFrame from '~/components/DemoFrame.astro';
import BasicVue from '~/components/demos/sankeydiagram/BasicVue.vue';
import BasicVueSource from '~/components/demos/sankeydiagram/BasicVue.vue?raw';
import DragVue from '~/components/demos/sankeydiagram/DragVue.vue';
import DragVueSource from '~/components/demos/sankeydiagram/DragVue.vue?raw';

## Basic usage

Data is passed via props and rendered as pure SVG, with no third-party charting dependency.
Colors are drawn from the `--viz-1..8` tokens and are colorblind-friendly.

<DemoFrame
  vueCode={`<CfSankeyDiagram ... />`}
  reactCode={`<CfSankeyDiagram ... />`}

  vueSource={BasicVueSource}>
  <BasicVue client:load />
</DemoFrame>


## Three-layer purchase path

Search engine / direct visit → product list / detail → add-to-cart / checkout, in three layers.

import SimpleVue from '~/components/demos/sankeydiagram/SimpleVue.vue';
import SimpleVueSource from '~/components/demos/sankeydiagram/SimpleVue.vue?raw';

<DemoFrame
  vueCode={`<CfSankeyDiagram :nodes="n" :links="l" />`}
  reactCode={`<CfSankeyDiagram nodes={n} links={l} />`}

  vueSource={SimpleVueSource}>
  <SimpleVue client:load />
</DemoFrame>

## Node drag

`draggable` defaults to `true`. Press and drag any node vertically to manually reorder its layer; links update in real time. Subscribe to `@node-drag` / `onNodeDrag` for the drop-position delta. Set `draggable={false}` for a read-only view.

<DemoFrame
  vueCode={`<CfSankeyDiagram :nodes="nodes" :links="links" @node-drag="onDrag" />`}
  reactCode={`<CfSankeyDiagram nodes={nodes} links={links} onNodeDrag={onDrag} />`}
  vueSource={DragVueSource}>
  <DragVue client:load />
</DemoFrame>

## API

| Prop | Type | Default | Description |
|---|---|---|---|
| `nodes` | `SankeyNode[]` | `—` | &#123; id, name, layer?, colorIndex? &#125;[] |
| `links` | `SankeyLink[]` | `—` | &#123; source, target, value &#125;[] |
| `nodeWidth` | `number` | `12` | Node bar width |
| `draggable` | `boolean` | `true` | Allow vertical drag to reorder nodes within a layer |
