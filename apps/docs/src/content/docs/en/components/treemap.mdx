---
title: Treemap
description: Hierarchical rectangle area chart, single-level slice-and-dice layout.
---

import DemoFrame from '~/components/DemoFrame.astro';
import BasicVue from '~/components/demos/treemap/BasicVue.vue';
import BasicVueSource from '~/components/demos/treemap/BasicVue.vue?raw';
import NestedVue from '~/components/demos/treemap/NestedVue.vue';
import NestedVueSource from '~/components/demos/treemap/NestedVue.vue?raw';

## Basic usage

Data comes in via props; rendered as pure SVG with no third-party charting dependency.
Colors come from the `--viz-1..8` tokens, colorblind-friendly.

<DemoFrame
  vueCode={`<CfTreemap ... />`}
  reactCode={`<CfTreemap ... />`}

  vueSource={BasicVueSource}>
  <BasicVue client:load />
</DemoFrame>


## Label visibility

`showLabels=false` shows only color blocks — useful for thumbnail layouts. Text only appears when a rectangle is large enough (w>50, h>20).

import LabeledVue from '~/components/demos/treemap/LabeledVue.vue';
import LabeledVueSource from '~/components/demos/treemap/LabeledVue.vue?raw';

<DemoFrame
  vueCode={`<CfTreemap :nodes="nodes" />
<CfTreemap :nodes="nodes" :show-labels="false" />`}
  reactCode={`<CfTreemap nodes={nodes} />
<CfTreemap nodes={nodes} showLabels={false} />`}

  vueSource={LabeledVueSource}>
  <LabeledVue client:load />
</DemoFrame>

## Nested hierarchy + drill-down

Attach `children` to any node and the layout recurses with slice-and-dice; parent rectangles reserve `headerHeight` pixels at the top for their label. Click a top-level rectangle with children to drill in: the subtree becomes the new focus, with a breadcrumb, an ↑ Up button, and a footer tracking the current focus. Pass `drillable={false}` for a static nested view.

<DemoFrame
  vueCode={`<CfTreemap :nodes="nodes" :width="640" :height="320" @drill="onDrill" />`}
  reactCode={`<CfTreemap nodes={nodes} width={640} height={320} onDrill={onDrill} />`}
  vueSource={NestedVueSource}>
  <NestedVue client:load />
</DemoFrame>

## API

| Prop | Type | Default | Description |
|---|---|---|---|
| `nodes` | `TreemapNode[]` | `—` | &#123; name, value?, colorIndex?, children? &#125;[] |
| `width / height` | `number` | `480 / 240` |  |
| `childPadding` | `number` | `4` | Inner padding around children inside a parent |
| `headerHeight` | `number` | `16` | Space reserved at the top of a parent for its label |
| `drillable` | `boolean` | `true` | Click a top-level rect with children to drill in |
| `showBreadcrumb` | `boolean` | `true` | Render breadcrumb + ↑ Up control when drilled |
