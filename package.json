{
  "name": "chukit-workspace",
  "private": true,
  "version": "0.0.0",
  "description": "ChuKit UI monorepo — 初见即用的基础组件库",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "pnpm -r --parallel --filter=./packages/* run dev & pnpm --filter=docs dev",
    "dev:vue": "pnpm --filter=@chukit/vue dev",
    "dev:react": "pnpm --filter=@chukit/react dev",
    "dev:docs": "pnpm --filter=docs dev",
    "build": "pnpm -r --filter=./packages/* run build && pnpm --filter=docs build",
    "build:packages": "pnpm -r --filter=./packages/* run build",
    "build:docs": "pnpm --filter=docs build",
    "clean": "pnpm -r exec rimraf dist .astro node_modules"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  }
}
